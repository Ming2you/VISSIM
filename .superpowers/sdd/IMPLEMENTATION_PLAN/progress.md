# SDD ledger - plan: C:/tmp/vissim-pstack-controller/IMPLEMENTATION_PLAN.md

> **2026-08-07 - the active plan is now `IMPLEMENTATION_PLAN_V3_LEAN.md`, not v2.1.**
> Entries below Task 5 were produced under v2.1 and remain valid as a record of what was
> built and verified. They are NOT a backlog to finish. v3 deliberately drops the evidence
> provenance layer (immutable manifests, hash chains, replay validator, ACL staging, signed
> release, wave retirement) while keeping every physical, signal, gradient, and runtime
> invariant. Read the v3 preamble before restoring anything from v2.1.

- Baseline: cb3c44d170b7f818baae7af399fb65c93b6fb1e3
- Working branch: codex/plant-fidelity-v2-1
- Baseline tests: scripts/tests 21/21 PASS; tests 9/10 PASS, stale snapshot expectation at tests/test_vissim_stackelberg_adapter_fidelity.py:75 (`35a5c82` vs `0240ba8`).
- Task 1: complete - three independent reports produced (signal/reproducibility, physical/state, dynamic/SPSA/runtime).
- Task 1 adjudication: signal report's SC12 left-only conclusion is rejected. Connector 10241/10238 each has two connector lanes and carries source lane 2 through traffic, while 10242/10240 carries lane 2 left traffic. Heads 50201/50601 therefore govern shared through+left lanes. Plan must require one shared physical stock and SG/stage coupling; it must not claim left-only geometry.
- Task 2: complete - IMPLEMENTATION_PLAN.md rewritten as v2.1; SC12 user correction integrated.
- Task 3: in progress - independent whole-plan cross-review and fix loop
- Task 3 fix round 1/5: accepted transfer-ledger, one-shot certification, action-clock, per-stratum support, SPSA-unit/statistics, end-to-end runtime, boundary-backpressure, command/test, dependency, traceability, and production-deliverable findings.
- Task 3 adjudication: rejected the spec review's SC12 left-only finding because its review brief was stale. User correction plus connector 10241/10238 two-lane definitions establish shared through+left lane 2; retained the adversarial review's profile-policy correction.
- Task 3 fix round 2/5: corrected 6+9=15 parent cardinality, added BaselineOnly isolation, split E-fit/E-cert process/ACL, completed signed-bias/spillback/SPSA thresholds, removed placeholders, and expanded the seven-field execution matrix.
- Task 3 fix round 3/5: added restricted E-fit wrapper, explicit CERT-PREP/CERT-WAVE producers, preregistered E/I/J/K consumers, one embargoed certification campaign, and corresponding dependency edges.
- Task 3 fix round 4/5: separated request specifications from post-run hashes; moved every certification input/intermediate output under one ACL staging root; added signed atomic CERT-RELEASE after K.
- Task 3 fix round 5/5: aligned E-fit DeniedCertificationRoot with the actual ACL staging root.
- Task 3: complete - scoped review clean after five recorded rounds; starting fresh whole-plan review.
- Task 4: pending - implement approved fidelity work in dependency order
- Task 4 S1: complete - canonical signal/compiler and SC12 shared-lane contract implemented; fix round 1/5 addressed all four independent findings with zero new Critical/Important findings (plant 82/82, scripts 65/65 at S1 close).
- Task 4 S0R source: fix round 1/5 implemented immutable upstream commit/tree/blob anchor, strict-by-default runtime verification, and actual n41 watchdog preflight identity; independent re-review pending together with baseline evidence fixes.
- Task 4 S0R baseline: fix round 1/5 in progress - bind snapshot PASS to exact runtime/preflight/audit and deepen no-control/COM/.err/Python evidence.
- Task 4 S0R source/baseline: complete after fix round 3/5 - all original C1-C3/I1-I4 findings independently ADDRESSED, zero new Critical/Important findings; actual VISSIM COM execution remains an explicit runtime verification gap rather than PASS.
- Task 4 A-B: in progress - deterministic physical topology/ownership and mass-conserving state projection.
- Task 4 A1: complete after fix round 3/5 - 339 real routes resolved, connector coverage 100%, ten XML-order shuffles deterministic, 14 independent graph/route mutations fail closed, final review PASS/APPROVED.
- Task 4 A2: complete after fix round 2/5 - 2,649 lanes compile to 7,275 unique one-stock intervals and 7,418 edges with exact cover; trusted evidence/partition identity, decision-scoped multi-owner shares, global artifact keys, SC12 shared-lane equality, and objective-trace tests independently PASS. Original 4 findings and the review-package path finding are ADDRESSED; final review Critical/Important 0, A1+A2 43/43 PASS.
- Task 4 B1a: in progress - initial per-vehicle A2 stock projection only. Architecture brief READY after five adversarial review rounds (all 22 original and follow-up findings closed, final new Critical/Important 0); live COM and performance remain explicit NOT_EVALUATED gates, and B1b/subsequent B tasks stay blocked.
- Task 4 B1a VBS capture: complete after fix round 2/5 - recurring epoch, zero stopped keys, reachable failure counters, called-path mutation resistance, dotted invocation, and complete review evidence independently APPROVED; live COM remains NOT_EVALUATED.
- Task 4 B1a core: residual C2 malformed/stale-output and duplicate live-state fixes independently ADDRESSED (28/28 focused tests); I3/I4 delegated to runner/live-trust integration.
- Task 4 B1a runner/live-trust architecture: READY after user-authorized exceptional fix round 6 - I3/I4 and I14-I17 all independently ADDRESSED; final rereview PASS with new Critical/Important 0. Live COM and live p95 remain NOT_EVALUATED; implementation slices pending.
- Task 4 B1a runner/live-trust slice 1: fix round 1/5 - original H1-H3, M1-M3, and L1 all independently ADDRESSED; 142 final-tree regressions PASS and git diff --check PASS. One new Important remains: the exported run-manifest validator imports a top-level script unavailable from an ordinary plant-package environment.
- Task 4 B1a runner/live-trust slice 1: fix round 2/5 - package-stable public validator finding ADDRESSED; 143 implementer regressions and 30 controller-focused tests PASS. One new Important remains: approval-replay child framing uses ASCII despite valid non-ASCII Windows workspace/path support.
- Task 4 B1a runner/live-trust slice 1: fix round 3/5 - non-ASCII approval-replay framing finding independently ADDRESSED; strict canonical UTF-8/LF wire and Korean workspace/package-boundary test PASS, new Critical/Important 0.
- Task 4 B1a runner/live-trust slice 1: complete (baseline cb3c44d..working tree, review clean). Immutable run-manifest, supported-version policy, monotonic helper, official closed preflight role universe, package-stable independent approval replay, and concurrent atomic publication are ready. Live COM/p95 remain NOT_EVALUATED.
- Task 4 B1a runner/live-trust slice 2: in progress - adapter projection-only path, strict projection-sidecar/reference validation, and normal-path prevalidated reference consumption.
- Task 4 B1a runner/live-trust slice 2 review: FAIL - 4 Critical (type-coherent numeric mutation, stale PASS publication, parsed-byte/hash version split, destructive output aliasing) and 5 Important (duplicate projection algorithm, ledger not truly consumed, incomplete action provenance, inadequate mutation/ordering matrix, huge-integer OverflowError) enter fix round 1. One shallow-immutability Minor is deferred to final review unless touched by the fix.
- Task 4 B1a runner/live-trust slice 2 fix round 1/5: 6/9 Critical/Important findings independently ADDRESSED; 26 focused + 52 B1a/core controller reruns PASS. Open: parser-stage failures can retain stale PASS; pre-invalidation immutable roles omit approval-bound A1 lane graph; focused matrix lacks those two paths. No new Critical/Important breakage. Snapshot-value immutability Minor remains partially open/deferred.
- Task 4 B1a runner/live-trust slice 2 fix round 2/5: exact parser-error/A1 authorization paths implemented and 33 focused tests PASS, but rereview found argparse long-option abbreviation mismatched the raw preparser. Original Critical 2/4 and Important 4 remain open; abbreviated `--state-json` can newly omit the state role before unlink. No other new Critical/Important finding. Enter fix round 3 with exact CLI grammar alignment.
- Task 4 B1a runner/live-trust slice 2 fix round 3/5: long-option abbreviation disabled; Critical 4, Important 4, and abbreviated-state regression independently ADDRESSED; 38 focused controller rerun PASS. Original Critical 2 remains: split `--out-projection-reference -victim.json` is rejected by argparse but raw preparse treats the single-dash token as a value and may unlink it. Enter round 4 with fresh implementer and parser-token classifier as the confirmed root cause.
- Task 4 B1a runner/live-trust slice 2 fix round 4/5: shared full-parser token classification closes the residual single-dash split-value Critical; 41 focused tests, 155 reported non-overlapping regressions, and 3 controller reruns PASS. Independent rereview Requirements PASS / Code quality APPROVED, new Critical/Important 0.
- Task 4 B1a runner/live-trust slice 2: complete (baseline cb3c44d..working tree, review clean). Projection-only validates/publishes one sidecar/reference before NumSim; required normal path validates and consumes the exact ledger before runtime/fallback and emits complete JSON/CSV projection provenance. Live COM/p95 remain NOT_EVALUATED.
- Task 4 B1a runner/live-trust slice 2 minor (deferred): `BoundedJsonSnapshot.value` is not recursively frozen; validated artifact/state/sidecar views are frozen. Final whole-branch review must triage before merge.
- Task 4 B1a runner/live-trust slice 3: in progress - watchdog/VBS required-mode manifest, environment, atomic state/capture/projection/timing transaction and live-evidence integration.
- Task 4 B1a runner/live-trust slice 3A: implemented pending independent review - required watchdog campaign/attempt identity, exclusive attempt-local config/output roots, immutable run-manifest create/reload, closed-source rehash via the pinned Python producer, environment transaction/restoration, and strict-matrix required propagation are covered by 147 focused no-VISSIM regressions. VBS capture companions, projection timing/reference invocation, post-run v2.2/replay, live COM, and p95 remain deferred/NOT_EVALUATED.
- Task 4 B1a runner/live-trust slice 3A fix round 1: IMPLEMENTED_PENDING_INDEPENDENT_REREVIEW - addressed prior 2 Critical + 6 Important findings: bounded StallSec monitor/kill/retry, executed config pre/post byte checks, VBS-mode capture schedule, evidence-free dry-run/NOT_EVALUATED matrix propagation, producer-exclusive absent attempt directories, child-only environment overrides preserving parent env, legacy matrix mode, and process-level no-COM tests. Verification: watchdog/manifest/matrix 56/56 PASS; B1a core + VBS static + compiler/signal 40/43 PASS in sandbox with only cscript settings denial, then VBS helper 3/3 PASS outside sandbox; adapter 2/2 PASS; projection/reference 50/50 PASS; git diff --check PASS with line-ending warnings only. Live COM, required live execution, and p95 remain NOT_EVALUATED.
- Task 4 B1a runner/live-trust slice 3A fix round 2: IMPLEMENTED_PENDING_INDEPENDENT_REREVIEW - closed the blocking JSON numeric contract gap for watchdog-produced `allowed_capture_times`. Production PowerShell templates now emit JSON doubles (`1.0`) while preserving sorted unique integral epochs; `--validate-template-stdin` now builds and strict-validates the in-memory run manifest without publishing PASS evidence; no-COM integration covers production template -> pinned request -> create -> validate-only with a PASS synthetic fixture and rejects integer schedules in no-write validation. Verification: targeted integration PASS, watchdog/manifest/matrix 57/57 PASS, git diff --check PASS with line-ending warnings only. Live COM, required live execution, and p95 remain NOT_EVALUATED.
- Task 4 B1a runner/live-trust slice 3A: complete after fix round 2 - fresh independent rereview APPROVED with Critical/Important/Minor 0; watchdog campaign/attempt ownership, bounded timeout/retry, immutable manifest, mode-exact capture schedule, executed-config byte binding, child-only environment, evidence-free dry-run, and process-level no-COM coverage are accepted. Focused 57/57 PASS. Live COM and p95 remain NOT_EVALUATED.
- Task 4 B1a runner/live-trust slice 3B: in progress - required VBS run binding, atomic state publication, pinned monotonic raw-capture timing, and exact vehicle-capture-evidence sidecars. Projection-only/normal action, combined timing receipt, post-run v2.2, replay, and B1b remain deferred.
- Task 4 B1a runner/live-trust slice 3B: IMPLEMENTED_PENDING_INDEPENDENT_REVIEW - required VBS startup/run/time binding, exact state provenance, same-directory no-clobber state publication, pinned monotonic helper endpoints, raw capture request, exact create-once vehicle-capture-evidence producer/validator in the pinned state_manifest_builder role, deterministic topology-aware <=64 samples, and no-COM called-path tests are implemented. Verification: 178/178 focused/adjacent regressions PASS, cscript full-file parse reached usage exit before COM, git diff --check PASS with line-ending warnings only. Live COM, supported-version/sample live coverage, combined timing/p95, post-run v2.2/replay, and B1b remain NOT_EVALUATED.
- Task 4 B1a runner/live-trust slice 3B review: CHANGES_REQUIRED - 1 Critical (real A1/A2 string `link_no` rejected), 2 Important (unbounded required Python subprocesses; state renamed before strict validation), and 1 Minor report overclaim. Fix round 1 also covers main-review legacy provenance compatibility and the missing boundary/mutation/concurrency test matrix.
- Task 4 B1a runner/live-trust slice 3B fix round 1: IMPLEMENTED_PENDING_INDEPENDENT_REREVIEW - string `link_no` topology compatibility, bounded required Python helper calls, temp/final state run-binding validation before/after immutable rename, legacy two-field provenance compatibility, exact PASS stdout/stderr framing, temp cleanup, and expanded no-VISSIM mutation/20k/state-encoding tests implemented. Verification: focused 31/31 PASS; vehicle-capture 9/9 PASS; VBS helper 3/3 PASS outside sandbox; watchdog 10/10 PASS; run-manifest 36/36 PASS; B1a core 19/19 PASS; projection/reference 50/50 PASS; adapter/audit 33/33 PASS; compiler/signal 11/11 PASS; git diff --check PASS with line-ending warnings only. Live COM, supported real VISSIM readback, live connector/multilane sample coverage, combined timing/p95, post-run v2.2/replay, and B1b remain NOT_EVALUATED.
- Task 4 B1a runner/live-trust slice 3B fix round 1 rereview: CHANGES_REQUIRED - 1 Critical (valid-body BOM accepted on required state/capture paths), 2 Important (unbounded huge capture integers; missing concurrent create-once test), and 1 Minor broad-suite reproduction limit. Original topology/timeout/legacy findings remain addressed; enter fix round 2.
- Task 4 B1a runner/live-trust slice 3B fix round 2: IMPLEMENTED_PENDING_INDEPENDENT_REREVIEW - required state/request/sidecar byte paths now reject valid-body UTF-8 BOM without changing the shared legacy loader; capture evidence integer domains are bounded in producer and validator (positive signed 32-bit IDs, 20,000 counts, positive signed 64-bit monotonic endpoints); real concurrent create-once publish test proves exactly one immutable winner. Verification: focused 34/34 PASS; vehicle-capture 12/12 PASS; VBS helper 3/3 PASS outside sandbox after sandbox WSH settings denial; watchdog 10/10 PASS; run-manifest 36/36 PASS; B1a core 19/19 PASS; projection/reference 50/50 PASS; adapter/audit 33/33 PASS; compiler/signal 11/11 PASS; git diff --check PASS with line-ending warnings only. Live COM, supported real VISSIM readback, live connector/multilane sample coverage, combined timing/p95, post-run v2.2/replay, and B1b remain NOT_EVALUATED.
- Task 4 B1a runner/live-trust slice 3B fix round 2 rereview: CHANGES_REQUIRED - Critical 0, Important 2 (required state reads lack the existing byte bound; state `position_m`/`speed_kph` permit bool/string coercion), Minor 0. BOM, huge-integer, concurrency, and all prior findings remain addressed; enter fix round 3 with raw-lane binding hardening.
- Independent regression fix: shared B1a required/synthetic parent directory creation is now race-safe/idempotent via `New-B1aSharedDirectory`, preserving exclusive campaign/attempt semantics; verification PASS - concurrent same-outdir regression 10/10 repeated runs, full `test_b1a_watchdog_attempt_launch` 10/10 PASS. No VISSIM/COM.
- Independent regression fix rereview: APPROVED - watchdog shared-parent race review Critical/Important/Minor 0; repeated targeted 10/10 and full watchdog 10/10 PASS. Code unchanged in Slice 3B fix round 3.
- Task 4 B1a runner/live-trust slice 3B fix round 3: IMPLEMENTED_PENDING_INDEPENDENT_REREVIEW - all required state reads in capture production, reusable validation, and CLI state-run-binding now use the existing 8 MiB state byte bound; state vehicle records reject coercible/tampered numeric fields, nonfinite tokens, shape drift, invalid stopped derivation, negative speed, and position below tolerance; preserved `lane_raw` is independently reparsed with the VBS grammar and must agree with parsed link/lane IDs. Verification: vehicle-capture 15/15 PASS; VBS static + watchdog adjacent 22/22 PASS; VBS helper 3/3 PASS outside sandbox after WSH settings denial; git diff --check PASS with existing line-ending warning only. Live COM, supported real VISSIM readback, live connector/multilane sample coverage, combined timing/p95, post-run v2.2/replay, and B1b remain NOT_EVALUATED.
- Task 4 B1a runner/live-trust slice 3B fix round 3 rereview: CHANGES_REQUIRED - Critical 0, Important 1 (producer/reusable validator do not rederive full-network link/stopped maps and envelope count identities from strict records), Minor 0. Every prior Slice 3B finding remains closed; enter fix round 4.
- Task 4 B1a runner/live-trust slice 3B fix round 4: IMPLEMENTED_PENDING_INDEPENDENT_REREVIEW - producer and reusable validator now share one vehicle-records envelope validator that enforces exact field completeness/schema/source attributes/stopped threshold, bounded scalar counts, zero unobservable/external counts, strict record uniqueness, rederived full-network link/stopped maps from records, required zero stopped keys for occupied links, and root total/stopped agreement when present. Added producer+validator tamper coverage for wrong/missing/extra link and stopped keys, bool/string/negative/oversize map values, noncanonical keys, scalar drift, envelope extra/missing fields, root total drift, empty valid capture, and stopped-threshold boundary. Verification: vehicle-capture 17/17 PASS; VBS static + watchdog adjacent 39/39 PASS; VBS helper 3/3 PASS outside sandbox after WSH settings denial; run-manifest 36/36 PASS; B1a core provenance 19/19 PASS; git diff --check PASS. Live COM, supported real VISSIM readback, live connector/multilane sample coverage, combined timing/p95, post-run v2.2/replay, and B1b remain NOT_EVALUATED.
- Task 4 B1a runner/live-trust slice 3B fix round 4 rereview: CHANGES_REQUIRED - Critical 0, Important 1 (required envelope still accepts coercive threshold/time/zero-count types and omitted root total/stopped fields), Minor 0. Bad map replay and every earlier finding remain closed; enter fix round 5 with the same validator added to pre-rename CLI state binding.
- Task 4 B1a runner/live-trust slice 3B fix round 5: IMPLEMENTED_PENDING_INDEPENDENT_REREVIEW - one exact required-state envelope validator now covers producer, reusable validator, and CLI pre-rename state binding. It rejects bool/int coercion for JSON-double timing/threshold fields, rejects bool count scalars, requires root `total_vehicles` and `stopped_vehicles`, and preserves optional/required distinction in state-manifest aggregation. Verification: vehicle-capture 18/18 PASS; focused Slice 3B/VBS/watchdog 40/40 PASS; VBS helper 3/3 PASS outside sandbox after sandbox WSH settings denial; `git diff --check` PASS on final file state with line-ending warnings only. Adjacent B1a core fixture initially failed after the stricter contract because it omitted required `stopped_vehicles`; fixture aligned, no further broad suites run per user instruction. Live COM, supported real VISSIM readback, live connector/multilane sample coverage, combined timing/p95, post-run v2.2/replay, and B1b remain NOT_EVALUATED.
- Task 5: pending - whole-branch verification and final review
- Task 4 B1a runner/live-trust slice 3B fix round 6 (Claude, 2026-08-06): ADDRESSED the fix5 rereview's single open Important. Root cause: `validate_state_run_binding` was used under two different contracts. fix5 passed `require_vehicle_records` from `build_state_manifest_v2_1.py:1116`, promoting vehicle-record envelope problems to run-binding problems, so `build_manifest()` returned 1 before projection validation could inventory the state and replace stale projection/audit sidecars with FAIL evidence. The intended split is documented by the adjacent test itself (`build_manifest()==0` inventory, then `validate()==1` emits the FAIL sidecar) and is already implemented downstream: `validate_state_projection_v2_1.py:451` validates binding only and `:469` decides FAIL/NOT_EVALUATED from `required_vehicle_records`. Fix: dropped `require_vehicle_records` from the manifest-build call only. The three paths that must reject a bad required envelope immediately keep `require_vehicle_records=True` — producer (`:635`), reusable validator (`:742`), CLI `--validate-state-run-binding` (`:1306`). Verification: `scripts.tests.test_b1a_core_provenance` 3 failed -> 19/19 PASS (the rereview counted 5 because three were subTest cases of one test); `plant/tests` 132/132 PASS; adjacent `scripts/tests` + `tests` 238 passed / 8 failed, and all 8 reproduce identically with the fix reverted (pre-existing). Those 8 are a Korean-path environment defect, not a code defect: `UnicodeDecodeError: 'utf-8' codec can't decode byte 0xc7` when decoding PowerShell output under a CP949 workspace path; they pass in the `C:/tmp` worktree whose path is ASCII. Live COM, supported real VISSIM readback, live connector/multilane sample coverage, combined timing/p95, post-run v2.2/replay, and B1b remain NOT_EVALUATED.
- Test execution convention (discovered): `plant/tests` must run with cwd=`plant/` AND the repository root on `PYTHONPATH`. `plant/tests/*` import `src.vissim_strict...` (needs `plant/`) while `test_vissim_strict_physical_projection_reference.py` imports `scripts.tests.test_build_preflight_manifest` (needs the repo root). Running from either root alone produces collection errors.
- Corrections to the fix6 entry above (Claude, 2026-08-07): (a) "봉투 판정은 이미 하류에 있다" is true only for an **absent** envelope. `validate_state_projection_v2_1.py:469` sits inside the `:466` `if "vehicle_records" not in state:` branch; a present-but-tampered envelope is judged by `:492` `normalize_vehicle_records`, which did not read root `stopped_vehicles`. (b) The cited kwarg line was `build_state_manifest_v2_1.py:1117`, not `:1116`. (c) "세 경로 모두 즉시 거부" overstates: producer `:635` and reusable validator `:742` call `_state_records_by_no` unconditionally on the next line, so only the CLI `:1306` depends on the flag for rejection. (d) The claimed "8 pre-existing Korean-path CP949 failures" does not reproduce; see the fix7 report.
- Task 4 B1a slice 3B fix6 independent rereview + Task 5 whole-branch review (Claude, 2026-08-07): CHANGES_REQUIRED / CHANGES_REQUIRED. Eight read-only lenses (3 fix6, 5 whole-branch) with per-finding adversarial refutation; 20 of 28 Critical/Important findings were refuted, most of them NOT_EVALUATED gaps relabelled as defects. Four Important survived and were each independently re-verified by the author against the code: root `stopped_vehicles` unchecked across the manifest->projection path, sidecar filenames colliding with the audit `state_*.json` discovery glob, the dry-run template serializer skipping the publish path's JSON-double normalization, and `.superpowers/sdd/.gitignore` `*` hiding all future ledger files. fix6's direction is correct and is not reverted. Record: `task-b1a-slice3b-fix6-rereview-and-task5-review.md`.
- Task 4 B1a slice 3B fix7 independent rereview (Claude, 2026-08-07): CHANGES_REQUIRED - F1/F3/F4 closed, F2 only half closed. Important 2 survived adversarial refutation and were re-verified by the author: (a) the sidecar exclusion landed only in `audit_plant_fidelity.py`, while `validate_baseline_snapshot.py:780-787` re-globs the same directory unfiltered and compares exactly against the audit's `state_file_count`/`state_files`, so the pipeline still FAILs under the very condition F2 targeted - only the reason changes; (b) fix7's F3 revert-proof does not hold as stated - it reverted the *harness* serialization, not the `.ps1` dry-run call site, and no test in the repository exercises `Invoke-B1aTemplateValidationNoWrite`, so reverting that one line leaves the suite green. Nine Minors also accepted. Both blocking findings confirmed against the code before acceptance.
- Task 4 B1a slice 3B fix round 8 (Claude, 2026-08-07): IMPLEMENTED_PENDING_INDEPENDENT_REREVIEW - both rereview Importants and six Minors ADDRESSED. F5: `validate_baseline_snapshot.py` now applies the same exclusion at both discovery sites, referencing `_current_auditor().STATE_SIDECAR_SUFFIXES` so the suffixes exist in exactly one place. F6: `test_dry_run_path_uses_the_shared_template_serializer` pins both call sites in source - shared-serializer call present, direct `ConvertTo-Json` absent. A behavioural variant that made the harness actually invoke the dry-run function was built and then rejected: it needs a BOM-written harness for the Korean path, and even then the grandchild python exits with zero output under the PowerShell tool while passing 5/5 under Bash; the sandbox hypothesis was tested and refuted, the cause was not identified, and an environment-dependent test would trap the next session. Minors: `state_stopped_mismatch` added to the closed reason vocabulary; stopped-identity population asymmetry documented (total uses `raw_count`, stopped counts only validated records - no false PASS because a dropped record already means FAIL); two negative cases (`wrong_stopped`, `missing_stopped`) added to plant, which previously had none for this identity; audit citation corrected to `:685` and the "audit reads it" claim downgraded - the audit records but does not judge; `.gitignore` widened to `!IMPLEMENTATION_PLAN/**`; recursive `assert_same_json_types` added because `assertEqual` cannot see `1` vs `1.0`. Accepted but deferred: F1 closes the post-hoc evidence path only - the live decision loop does not call `normalize_vehicle_records`. Verification: revert-proof for F5 (baseline `status` FAIL) and F6 (call-site assertion fails); `plant/tests` 132/132 PASS; `scripts/tests`+`tests` 250 passed / 1 failed under PowerShell, the failure being the same pre-existing `test_run_provenance_marks_missing_inputs`.
- Test shell convention (confirmed, 2026-08-07): `scripts/tests` must run from PowerShell. Under Bash, `test_run_plant_fidelity_matrix.py` fails 7 of 11 because those tests spawn `run_plant_fidelity_matrix.ps1` as a child; under PowerShell it is 11/11. Reproduced identically at HEAD `a9f6dea` with `git stash`, so it is shell dependence, not a code defect. This also resolves the "CP949 8 failures" discrepancy - that figure was measured under Bash and fix7's "1 failure" under PowerShell; neither was wrong. `plant/tests` is 132/132 in both.
- Task 4 B1a slice 3B fix round 7 (Claude, 2026-08-07): IMPLEMENTED_PENDING_INDEPENDENT_REREVIEW - all four findings ADDRESSED. Root `stopped_vehicles` identity mirrored beside the existing `total_vehicles` identity in `normalize_vehicle_records` (chosen over passing `require_vehicle_records` into the projection call, which would re-couple the two contracts fix6 just separated); two fixtures aligned to the real VBS producer, which writes both root counts at `run_real_world_stackelberg_controller.vbs:1517,1525`. Audit state discovery now excludes `STATE_SIDECAR_SUFFIXES`, guarded by a behavioural agreement test that calls both real sidecar path helpers. One `ConvertTo-B1aTemplateJson` now serves both the publish and dry-run paths; the .ps1 stays pure ASCII. Ledger `.md` files are un-ignored. Verification: revert-proof for all four (F1 `validate()` returned 2 with `status=PASS states=1`; F2 `state_file_count` 3 != 1; F3 compressed output emitted non-float `allowed_capture_times`; F4 probe `.md` invisible to `git status`); `plant/tests` 132/132 PASS; `scripts/tests`+`tests` 248 passed / 1 failed; envelope-building files 74/74 PASS; `git diff --check` PASS. The single failure is pre-existing - `test_run_provenance_marks_missing_inputs` fails identically at HEAD `b3794d0` under `git stash`, in `vissim_stackelberg_adapter.py:1129` `_git_commit`, a file this round does not touch. Round-cap deviation (5 -> 7) is justified in the fix7 report: these are four independent defects, not repeated attempts at one symptom. Live COM, supported real VISSIM readback, live connector/multilane sample coverage, combined timing/p95, post-run v2.2/replay, and B1b remain NOT_EVALUATED.

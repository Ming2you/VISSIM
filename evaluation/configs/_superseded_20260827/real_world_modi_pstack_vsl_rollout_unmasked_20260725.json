{
  "extends": "real_world_modi_pstack_crossgate_predcal_20260724.json",
  "name": "real_world_modi_pstack_vsl_rollout_unmasked_20260725",
  "description": "Prediction-calibrated P-Stack candidate with VSL candidate-specific METANET rollout scoring enabled and the bottleneck VSL mask disabled for freeway-stress validation.",
  "adapter": {
    "vsl_metanet_rollout": {
      "enabled": true,
      "method": "adapter runtime patch: rank freeway-follower VSL candidates using a link-level METANET/CTM rollout instead of reusing ramp-only freeway TTS terms"
    }
  },
  "config_overrides": {
    "freeway_follower": {
      "density_penalty": 80.0,
      "vsl_smoothness_weight": 0.0,
      "horizon_vsl_candidate_limit_per_link": 5,
      "max_vsl_step": 30.0
    },
    "leader": {
      "w_F": 3.0
    }
  },
  "actuation": {
    "vsl_segment_override_policy": "clear_when_mask_disabled",
    "active_lever_mask": {
      "enabled": false
    }
  },
  "notes": [
    "This config is intentionally diagnostic. It removes the 20260722 bottleneck VSL mask so all freeway segments can respond during a freeway-focused demand sweep.",
    "Use short smoke runs first. If VSL still remains at 120 kph under freeway breakdown, the remaining issue is not just mask or candidate scoring."
  ]
}

# Independent1093 pre-head review

No blocking conservation, step-order or accepted-flow attribution defect was found for the current single1093 declaration. Production source was read only. Hubble's6 focused tests passed in1.689s. Additional actual NC900 held-action450s coupled executions had identical Ω ON/OFF physical state, passed ledger stock closure at every interval, and left the input cfg/state/action/demand pickle unchanged. Evidence and reviewed source hashes are in `native_input_prehead_peer_review.json`.

The installed order is coherent: runtime initializes input observations, route tags and pre-head tags before ledger seeding. Each urban step advances tags first, then admits native generation. `limit_intended` bounds ordinary p4 demand by eligible queue stock before the shared receiving allocation. The ordinary queue debit, receiver debit and area event happen once; `_receive_corridor` then notifies the pre-head helper of **actual** acceptance before normal downstream arrival scheduling. `finish_step` runs after all relevant urban movement receipts. It subtracts actual W/N service from the p3 head budget, partitions only the remaining service among blocked left tags, and requires positive travel delay before p4 eligibility. The p3 progress changes tags only; it emits no extra stock or boundary event. Ordinary arrival scheduling remains responsible for final p4 receipts.

The proportional-receipt identity was also checked with the actual helper: queue9 comprises blocked tags4, eligible tags1+2 and eligible ordinary stock2. After ordinary accepted flow2 leaves queue7, the helper reconstructs eligible-before5 and removes0.4/0.8 from the two eligible tags; residual tags are0.6/1.2/4. Blocked tags remain unchanged. This confirms the denominator excludes upstream-blocked and still-travelling tags while retaining ordinary eligible vehicles.

The450s check generated7.5 tagged vehicles and completed6.0 from the declared subset. Additional left upstream-p3 service was0.5817174515 vehicles. Existing W/N service exceeded the reference two-lane upstream budget by11.5120736257 vehicles over the interval. This is a recorded limitation of the unchanged W/N aggregate service, not an extra capacity granted to left tags. The helper correctly assigns zero additional left service when that budget is exhausted. It does not establish complete two-head W/N discharge fidelity, local microscopic FIFO, observed route assignment for pre-existing shared-road vehicles, or capacity calibration.

Two narrow hardening observations were sent to Hubble; neither was shown to fail the current1093 data:

- `initialize` allowed an EPS-sized `n > occupied` discrepancy but computed `1−n/occupied` without a zero floor. Such rounding could create tiny negative old reservations. Clamp to the same nonnegative fraction convention used by `native_input_routes` while retaining the above-EPS failure.
- The one-prehead declaration guard inspected `cfg.network.native_internal_inputs`, but the owner builds the new `specs` locally and assigns that field after all inputs are configured. It therefore cannot reject a second prehead declaration during the same new build. Enforce the declared one-subset scope in the owner's evidence/input pass, without mutating cfg during partial validation.

Validation boundaries: the independent450s replay uses a historical NC held action and current explicit integration inputs. No optimizer or VISSIM was run. The comparison establishes implemented stock and stage consistency, not predictive accuracy or the unmeasured capacity of the native head. Parent is separately preparing the complete portable fixture suite and actual main preflight.

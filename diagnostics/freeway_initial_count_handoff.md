The unapplied `freeway_initial_count.patch` preserves each observed freeway cell's vehicle count in the density coordinates used by the vendor continuity equation. It changes three functions in the single adapter and only runs when `freeway.physical_vehicle_counts: true` is explicitly configured. Production files remain frozen and unchanged.

The original projection uses the observed W/E cell length to compute density, while the subsequent conservation equation uses `network.freeway_segment_length_km`. The smaller length discrepancy survived the earlier lane-field fix. Across the 31 pure-n7 observations it produced raw-minus-model FW discrepancies up to 0.311742 vehicles. The proposal computes each initial `rho_i = observed_N_i / (continuity_scalar_L * observed_effective_lanes_i)` and makes both vehicle-count getters invert those same coordinates. Physical per-cell length profiles remain available to speed and travel geometry. There is no aggregate correction, added stock, or later reseeding.

Apply `area_routes_and_sc2001.patch` once for the proposed route/corridor package, then `freeway_initial_count.patch`, `legsplit_single_transfer.patch`, `area_failfast.patch`, and `strict_decision_failfast.patch`. A joint `git apply --check` of these five files passes against the frozen checkout. The route package supersedes `dynamic_area_runtime.patch` and `sc2001_urban_flow_accounting.patch`; the single-transfer patch supersedes `legsplit_receiving.patch`. Do not apply superseded patches separately. All remain proposals until the live run ends and the parent reviews them.

Use the explicit deep merge of `control_area_physics_overlay.json` with the base n7 configuration and additionally set:

```json
{"freeway": {"physical_vehicle_counts": true}}
```

The physics overlay itself does not choose an objective. To score Ω residence and outward crossings, separately enable the existing `control_area_objective` block with an explicit `beta_seconds`, membership path `diagnostics/control_area_membership.json`, and route-contract path `diagnostics/control_area_route_contract_physical_routes.json`. No production beta is selected here. The checked candidates remain 0, 60, 150 and 300 seconds per vehicle, converted once to hours in `TTT_veh_h - beta_sec/3600 * TTD_veh`.

Validation completed:

- `python -m unittest diagnostics.test_freeway_initial_count -v`: 6 tests pass. Synthetic nonuniform physical lengths/lanes preserve every cell's N, both count getters match the continuity equation, fractional effective lanes remain valid, invalid scalar length raises, and absent/false flags preserve all actual NC900 projected state fields and legacy getter results exactly.
- `python diagnostics/probe_all_area_initial.py --support-path diagnostics/physical_projection_support_635_proposal.json --exact-freeway-counts --label physical635_exactN`: all 31 pure-n7 observations from 900 through 5400 seconds pass. Maximum per-cell FW floating-point error is 2.84e-14 vehicles; both FW totals and entire Ω totals match raw observations with maximum aggregate error 0.0. Positive urban physical projection gaps are 0. Each raw cell is checked independently, so cancellation between cells cannot satisfy this test.
- All 31 observations have zero separate modeled ramp demand and zero former shared69 gate demand. The shared69 native1101 input therefore does not duplicate an existing ramp or gate forecast in this traffic set.

The stronger endpoint command is:

```text
python diagnostics/probe_single_transfer_grid.py --support-path diagnostics/physical_projection_support_635_proposal.json --exact-freeway-counts --label physical635_exactN_singletransfer --timeout-sec 60
```

The final single-transfer version completed successfully: 31 independent observed states, five fixed-control candidates, four explicit beta coefficients, and one repeat of each opening candidate: **651 passing rollouts of 450 seconds**. All model-stock closures, no unresolved positive route events, state/candidate isolation, repeat-order invariance, coefficient-independent dynamics, the actual price-endpoint objective identity, and disabled invalid pruning/far extrapolation pass. `box_walk=False` deliberately fixes each candidate's requested lever for this coverage probe. Results are model tests, not live performance or optimal-beta evidence. `area_all_pure_n7_snapshots_physical635_exactN_singletransfer.json` has `complete=true`, 31 passes, 0 failures, and initial Ω residual 0 for every state. Each record explicitly identifies the unapplied single-transfer and exact-count proposals.

Beta changes the full ranking in 22 of 31 states and changes the first-ranked candidate in three states. At 1500 seconds, offset's TTT 429.312926 / TD 1648.138728 beats VSL's TTT 429.333664 / TD 1649.153260 when beta is 0, while beta 150 gives 360.640479 versus 360.618945 veh·h and reverses them. These final single-transfer results replace the earlier pre-fix numerical rankings. This is an actual candidate tradeoff produced by the installed endpoint, not a forced synthetic ranking; the known physical prediction errors still prevent using it as a beta recommendation.

The four flattened review configs are `diagnostics/area_candidate_configs/n7_area_beta0.json`, `n7_area_beta60.json`, `n7_area_beta150.json`, and `n7_area_beta300.json`. Reproduce them with `python diagnostics/prepare_area_candidate_configs.py`. The recipe deep merges the complete n7 base plus physics overlay, explicitly enables exact per-cell counts, adds each explicit area beta, asserts all prerequisite flags, and rejects a635 support document without its positive-unresolved guard. `manifest.json` pins source/patch/output SHA256 values. No live config is overwritten. The approved signal-clock contract is still pending; after it is supplied, rerun with `--clock-overlay diagnostics/<approved-overlay>.json`. The recipe does not invent its configuration key.

`python diagnostics/probe_area_full_decide.py --time 1200 --beta 0 --prepare-only` reaches the real adapter main's `decide_with_info` call with horizon3,10 price workers, exact Ω2402, finiteSC2001 and the proposed worker bootstrap. It loads the adapter directly; no adapter copy is created. Omit `--prepare-only` to execute that decision and write only under `diagnostics/area_main_preflight`. Fresh price workers import the same diagnostic package proposal before the canonical worker runtime installer. Preparation is not a completed MPC test, and the separate full-decision output must be checked for controller fallback before reporting success.

The first actual full-MPC run exposed a candidate omitted by the five fixed-lever grid. At predicted urban step315, R_D_W had152.569492 vehicles before the urban body and153.2(capacity) after other accepted receipts. The earlier legsplit calculation had reserved another0.223023055 vehicles from SC1001_W_out; the post-body `min(cap, queue + injection)` discarded them. The strict ledger correctly rejected that prediction, but the legacy main handler converted it to `fallback_fixed` and exit0. The first exact state/candidate is preserved in `area_main_preflight/first_ramp_clip_50548.pkl` with its JSON evidence.

The final `legsplit_single_transfer.patch` rechecks actual remaining ramp space after the urban body. Requests competing for that remaining space retain their existing proportional allocation; only accepted vehicles are withdrawn/emitted, and rejected vehicles stay in their source W_out. It also uses an explicit wrapper-only `defer_legsplit_sinks=True` argument so the generic W_out sink cannot remove those same vehicles earlier. There is no inferred vendor drain or undo correction. This second part matters: t3300/beta300 exposed an additional2.820671019-vehicle discrepancy when shared69 changed ramp space before the generic sink and invalidated the old pre-body drain estimate. That complete actual proxy candidate is saved in `area_main_preflight/failed_endpoint_25540.pkl`. `test_legsplit_single_transfer.py` passes both real failure replays and default-body equivalence. The old flag-OFF adapter/vendor path remains unchanged. Source space becomes available at the wrapper's actual receipt phase, which can conservatively delay upstream acceptance relative to the former early generic sink. Local owned ramps already update their queues immediately after each receipt; the same deferred-injection race is absent there. Frozen external-receiver assumptions remain a separate local-model approximation. After the final grid, obsolete undo-estimate comments were corrected without changing executable code: before/after location-free AST SHA256 is `f4e9efeacd97cfa429a4ea5443ef00d65975e80dfa08513af4729c623bd19b98`. The final patch hash is pinned in the regenerated config manifest.

`area_failfast.patch` extends the existing diagnostic-profile rethrow to any decision with `cfg.network.control_area_enabled`. Membership, runtime/bootstrap, or other decision failures cannot become a successful fixed-control result under that explicit mode. The exact extracted main handler passes2 tests covering strict original-exception propagation and absent/false legacy fallback equivalence. `strict_decision_failfast.patch` propagates the existing effective area-enabled setting through the watchdog to VBS. A strict normal-controller failure stops the simulation and exits3; disabled/absent settings reset stale inherited strict environment values and preserve legacy continuation. Demand_observation's6 tests/12 actual child-process and fake-COM cases pass; its handoff and manifest contain the independent evidence. Neither patch alone is the complete error-propagation chain.

With the final single-transfer fix, actual main t1200/beta0 completed **status=ok**,84.499871 seconds, and t3300/beta300 completed **status=ok**,72.754738 seconds. Both used10 configured price workers and0 serial reruns, producing diagnostic action JSON/CSV. These are solver preflights, not live performance. The final grid command is `python diagnostics/probe_single_transfer_grid.py --support-path diagnostics/physical_projection_support_635_proposal.json --exact-freeway-counts --label physical635_exactN_singletransfer --timeout-sec 60`.

Prediction fidelity remains a separate material limitation. With this full proposed physics fixture and the same actual action/current-rate demand as the parent's baseline probe:

| Initial time | E8 actual speed after150s | Baseline model | Corrected model | Actual density | Corrected density |
|---:|---:|---:|---:|---:|---:|
|1200|23.2732 km/h|96.4101|95.8960|48.1963 veh/km/lane|16.3805|
|3300|6.2690 km/h|23.3584|23.2552|101.7477 veh/km/lane|53.8680|

Thus inventory/projection fixes barely reduce the optimistic congestion recovery. `probe_corrected_prediction_fidelity.py` and `corrected_prediction_fidelity_two_states.json` reproduce the comparison. They do not include an FD change or resolve the separately reported signal timing/offset mismatch. NC13 empirical turn priors, lumped travel/receiving timing, conservative78 holding, and pending seed14 holdout remain explicit model limitations. The 45 physically ambiguous support elements stay unresolved and fail on any positive observation; current31 snapshot coverage does not prove universal635 support.

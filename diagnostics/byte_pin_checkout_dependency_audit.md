# Pre-normalization byte-pin and dependency audit

One runtime dependency was missing from both the99-source manifest and114 explicit attributes: `diagnostics/native_sc15_service_calibration.json`. Both active calibrated SC15 corridor declarations pin and load it through `route_choice_corridor._calibrated_native_service`. Its683 line endings are allCRLF and its SHA256 is `3119a382c0a207bea4bdd37f1a034b7a4d44f7b5eeae51e26510e2ea3898a80a`. Add it to explicit staging, manifest sources, and `text eol=crlf whitespace=cr-at-eol`. The underlying FZP is historical evidence metadata; this runtime validator reads the calibration's bounded blocks and does not open that FZP.

Within the manifest's actual scope, all99 source hashes and4 output hashes matched. Every one of those103 paths was covered by the114-rule block. Effective `git check-attr` showed no filter, working-tree-encoding or ident transformation on these files. Local Git has `core.autocrlf=true` and LFS globally configured, but none of the checked paths uses the LFS filter. Predicted clean/checkout round trips preserved all114 working files. A single read-only `git hash-object --stdin-paths` batch independently matched every predicted clean blob. No index, object, working source, attribute, native data or manifest was changed by this audit.

The12 mixed-ending paths comprise9 Python controllers, one JSON overlay, the watchdogPS1 and controllerVBS. Existing `-text` rules do preserve their raw bytes, but also expose previously normalized CRLF portions as large Git diffs. Parent's proposed **Python-only** LF normalization is supported: all9 have zero bareCR and exact AST equality before/after normalization, including string constant values. The8 files with a HEAD blob all have pureLF in HEAD; `native_internal_input.py` is new/untracked. `sc2001_corridor.py` becomes byte-identical to HEAD after normalization. Actual adapter changes ignoring EOL are54 insertions/6 deletions; runtime_setup42/1 and shared_approach10/5. Exact paths, counts, current and proposed hashes are in `mixed_python_normalization_review.json`. The JSON/PS1/VBS/native files were not proposed for this source-only normalization. Rebuild the manifest after the parent's normalization and keep the earlier900 preflight labelled with its original source hashes.

Static local import resolution includes Python namespace packages and found no missing production modules or imports of the8 retired proposal builders/modules. Every existing path in both prior staging lists exists, and all8 retired source paths remain absent. `physical_projection_support_635_proposal.json` remains a required live data artifact despite its historical filename and must be retained. The migrated diagnostics' static dependency closure also resolved; this is a source inventory, while parent owns the portable execution test.

`byte_pin_additional_stage_paths.json` gives50 explicit additional paths beyond the two prior lists and the105 changed/untracked paths in the combined current dependency inventory. It includes the new SC15 calibration, source/phase/native-route helpers and tests, fixturev2 ZIP/manifest, and active configuration outputs. These are paths to review and stage after the final manifest regeneration; the audit performed no staging. Twelve hashes in the older route-input handoff inventory now describe historical versions, so use that inventory for scope and the current manifest for current source integrity.

Recursive local `{path,sha256}` traversal found no other omitted nested dependency. It also found two old config/support fingerprints inside `positive_unresolved_connector_review.json`. Those identify that historical review's original inputs; they are not a current runtime nested-pin read and must not be rewritten to imply the current config produced the old evidence. Neither observation changes the completed historical evidence.

Limits: this checks the effective repository rules and current host's Git clean behavior, not arbitrary future user attribute/filter overrides. Static imports include conditional branches and do not imply all listed modules execute in every controller mode. Re-run the parent's manifest verifier after any authorized normalization, rather than applying this pre-normalization report's hashes as final runtime pins.

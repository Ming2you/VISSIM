# Objective and shared service integration

Both approved patches are applied, and **39 tests passed** against actual production imports in4.987 seconds: objective14 and shared25. Fresh-process checks passed for the objective class alias, shared actual green caller ON/OFF, and serialized ready-seed kernel. No proposal/builder module was loaded, and all production controller file hashes stayed unchanged during the final test execution. Exact source hashes, test counts and representative traces are in `objective_shared_production_validation.json`; full test names/results are in its `.log`.

Applied sequence was `area_leader_objective.patch` SHA`edf74d9204e764015f00e8bec00ebd90279124da915455e48df780b0dbdadea3`, then the superseding `shared_service_pool.patch` SHA`a4f1abf91fe6c8393fbd2c9c9408262803a108a0dbaf253a39ddb26672024f29`. Both passed applicability checks before application. Flow subsequently saved its calibrated route-resource consumer and Hubble saved its signal-observer hooks before this final test. Their changes were not edited by this agent. The test input remains the declared baseline fixture/current900 snapshot; this test does not claim a new full configured MPC decision.

The canonical objective helper preserves OFF, rejects unverified base proxy/fallback consumers, and keeps actual Wu full/proxy/PFO endpoint-score composition explicit. The actual adapter terminal wrapper and shared area installer are called directly. Endpoint response fixtures test call-path provenance and arithmetic; they do not execute a physical endpoint rollout. Leader and Nash objectives are not required to equal each other.

Shared tests execute the original green and offset callers with bounded candidates and the actual phase-refinement methods. They cover common accepted-only resource debits, physical green profiles, finite receiving, immutable ready seeds, pending residence, due boundaries, future10-second block admission and7-step maturation, ON/OFF lifecycle and nested/exception cleanup. SC1004 alone is added to the temporary phase-resolved active set, with the actual default flag False and exact restoration. Fresh workers use actual `runtime_setup.install_worker_runtime`. Existing documented local-horizon approximations remain; these tests do not establish complete global prediction fidelity or calibrate service capacity.

The two test files no longer contain an unapplied-source loader, automatic proposal fallback, generated ModuleType or source exec. Their original raw bytes were archived before conversion in `objective_shared_pre_import_tests.zip` (SHA`487542cb4d79abaa88b69b4ab86717c593db04572ee8410cb13f746212e96575`); CRC and both member-byte comparisons passed. The first migrated execution had one test-only `UnboundLocalError`: a loop variable named objective shadowed the imported module. Renaming it score was the only correction. `objective_shared_production_validation_attempt1.json/.log` preserve that failure; the final39-test execution passed without production edits.

Historical `shared_service_ready_validation.json` remains unchanged. Current shared evidence has the separate name `shared_service_ready_production_validation.json`. The2550 report now retains its original command values but withdraws the unproved hard realized-cap violation claim: continuous intent bounds, guard requests and discrete equivalent rates require distinct contracts. No scalar clipping, new capacity, routing prior or guard floor was introduced by these two patches.

Reproduction:

```text
python -X utf8 -m unittest diagnostics.test_area_leader_objective diagnostics.test_shared_service_pool -v
```

No VISSIM, full optimizer or coupled endpoint was run for this task. Parent owns fresh configured main preflight, subsequent simulation, Git index and commit. This agent changed only the six approved production targets, the two test modules and the listed diagnostic handoff/evidence artifacts; it did not modify previous configs or input evidence.

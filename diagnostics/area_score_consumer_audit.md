The n7 leader and its PFO incumbent use the same future rollout and leader penalties. The Ω reward is applied once in `area_runtime.evaluate_price_point`: replace global near TTT with ΩTTT minus beta/3600 times actual model outward crossings, preserve the other endpoint costs, and disable far/invalid partial-TTT pruning. `point.ttt` remains ΩTTT; `point.objective` is the score. Global freeway/urban TTT components remain separately available.

The active n7 consumer path is:

1. `StackelbergMPCController._leader_evaluation_base` (vendor `stackelberg_mpc.py:2400`) evaluates the candidate with the production endpoint. n7 has `leader_mfd_far_at_d0=True`; although the Ω wrapper disables actual far computation, that branch still selects `point.objective` as the base at2443. It does not add follower objective to that value.
2. `StackelbergWuMeteredController._evaluate_full_candidate` (`stackelberg_wu_metered.py:2624`) passes the returned base and future states to `leader.objective_terms` at2636. `LeaderController.objective_terms` (`leader.py:877`) uses follower mode base directly and adds existing target, storage, boundary, density and ramp penalties at928. No β or TD reward is applied again.
3. The n7 PFO incumbent uses the override `_evaluate_fallback_candidates` (`stackelberg_wu_metered.py:2155`), the same `_leader_evaluation_base` at2175 and the same future-state `objective_terms` at2182. It does not use the base controller's frozen `[state.copy()]*horizon` fallback function. That different behavior remains in other general Stackelberg modes and should not be assumed equivalent outside this n7 route.
4. The PFO tie-break (`stackelberg_wu_metered.py:2376`) compares those same full objectives. Their existing constraints/penalties are therefore comparable across leader and PFO; the optimized scalar is ΩJ plus preserved penalties, not a newly parameter-free objective.

Two additional consumers originally ignored the Ω objective: follower `_rollout_horizon_ttt` summed the global component TTTs, which both populated `NashResult.objective_value` and vetoed searched offsets with a0.5% global-TTT threshold; the leader fallback guard then compared `distributed_response_rollout_ttt` globally. The physical-signal production preflight at1200 observed offset on528.249365 versus off529.036236 veh·h, ten searched nonzero offsets and zero retained. Its leader fallback compared global529.036236 versus528.078881 while the actual leader Ω endpoint base was376.810295 and penalized objective440.199981.

The parent-authorized `area_follower_objective` fix makes the follower ranking return `point.objective`, uses strict additive ΩJ improvement for offset retention, and switches only area-enabled fallback to its existing objective comparison branch. Honest global and Ω TTT/TD/J diagnostics remain separate. The existing fallback objective comparator handles negative scores through additive differences and `abs(fallback_objective)` for its relative diagnostic threshold; severe stock/completion checks remain. This change must be included in the final actual main preflight and live source manifest. It is distinct from projection/stock-conservation tests, which alone could not expose the hidden selection consumers.

The subsequent actual3300/β300 return-frame trace found another selection-order issue: all four inner-to-outer follower returns changed the green vector, with33 changed phase values and42s maximum change in the first pair. That pair has `phase_price_in_gne=false` and no stored GNE overrides. Its actual cause is the later `apply_phase_price_refinement` in `LinkAgentWuFollower.solve` (`priced_wu_link_controller.py:737`), not GNE override insertion. The inner offset guard and recorded Nash objective therefore precede a material phase-vector change. The final leader/PFO endpoint still reruns the outer control, but that does not repair an earlier offset veto or make the recorded follower score describe the outer vector. Parent-owned ordering/scoring correction and final preflight are required before treating the complete follower path as consistent. Read-only evidence is `area_production_preflight/wu-link_t3300_beta300_20260909T183707416734Z/phase_trace/phase_commit_36844.jsonl`.

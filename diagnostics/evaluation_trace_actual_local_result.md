# Actual SC1004 local serialization probe

PASS: the original SC1004 phase setup/local cost returned the same result without tracing and under the proposed v2 monitor. Both cost encodings were exactly `0x1.4c58979ee9a45p+4`. The input state/action/demand pickle bytes stayed unchanged, trace validity passed with no parent/local errors, and all recorded source/input hashes stayed unchanged.

Evidence: `evaluation_trace_actual_local_20260910T042546182386Z/summary.json` and its parent/local JSON/JSONL sidecars. The reusable producer is `probe_evaluation_trace_actual_local.py`. It imports the actual `test_head_service_resources.configured` helper and original `test_shared_service_main_stack.local_result`, builds the actual canonical controller/terminal/worker-bootstrap stack, and executes one local evaluation in each mode from identical serialized initial controller packets. A named diagnostic root wrapper supplies actual cfg/state/controller context to the observer; it is explicitly not an invocation of the complete follower solve.

The input scope is the portable head-resource **v2 fixture configuration**, actual NC state at 900 seconds and prior action at 750 seconds, with the current head-resource contract enabled. It is a cold observation installation, not reconstruction of the seven-window learned floor history and not the current v4 full-MPC decision. The original raw900 action and demand feed both calls identically. No global endpoint, optimizer search, or VISSIM run occurred.

The real `LocalSignalModel`, current cfg/coupling, actual ready seed and the populated `_phase_ctx_cache` serialized successfully. The local companion recorded one physical-component enter/return plus two operational boundary snapshots; this only validates that narrow path, not complete candidate coverage. Final parent JSON was 1,602,377 bytes and local JSON was 1,203,031 bytes. The measured call sections were 0.02230 seconds without tracing and 0.14673 seconds with tracing; the complete probe took 4.28762 seconds. These single-call, overhead-inclusive figures do not predict full-MPC trace speed or normal benchmark performance.

Hubble independently read the final monitoring/local/finalize files and reported no additional blocking issue within callback ownership, selected-line handling, thread filtering and exception cleanup. That was a source review, not another execution. The existing v2 helper files, running v1 tracer, bootstrap, launcher, and production sources were not edited during this probe.

# Input1083 first-signal contract

The physical source21 must pass SC108 SG2, represented by the selected plan's p3, before reaching connector10112 and receiver174. Sending generation directly to `SC108_to_SC109` skips that service. At actual baseline900 the complete records contain pre-head vehicle5517 (lane1, 2.951m), post-head vehicle5429 (lane2, 160.846m), and connector10112 vehicle5422. A whole-link reassignment would therefore either bypass one vehicle's signal or charge an already completed signal again.

`physical_movement_routes.configure_native_input_signal_authority(cfg,tuning,detectors,*,state_json)` is implemented behind `urban.movements.native_input_signal_authority`. Point this declaration to `diagnostics/native_input_1083_signal_authority_ver2.json`. Call after dynamic origin repairs and movement capacity estimation, immediately before `native_internal_input.configure`. The validator requires the already installed physical signal contract and reads pinned network, selected-plan and area-membership bytes.

The exact checks are:

- Source21 has input1083 only, no incoming connector or source routing decision, two lanes, and one outgoing connector10112. Both lanes have their own fully applicable SC108 SG2 head at94.777881/95.050206m before branch168.104019m. Connector10112 carries two lanes into174 at75.408322m.
- The selected mainline SG plan assigns SG2 uniquely to live p3. The actual configured clock must match that pinned plan.
- The existing `SC108_W_to_E_SC109` still uses `in_SC108_W`, p3 and receiver`SC108_to_SC109`. The physical receiver has support174/173; the entire21→10112→174 path lies inside the declared area.
- No physical origin or positive movement observation and no external gate demand may already feed `in_SC108_W`. In the original detector379 was an additional origin support; the prior canonical dynamic repair removes it. The new validator fails if that repair was missed.
- The existing post-merge origin has exactly three expected movements. The through movement receives beta1; synthetic N and S siblings and their capacity/rename/visible-agent references are removed. Every other movement and every retained capacity remain unchanged.

The callable returns `(detectors, metadata)` and places a plain dictionary at `cfg.network.native_input_signal_authority`. Its `inputs['1083']` contains `physical_path`, `head_position_by_lane_m`, `pre_head_storage`, `post_head_storage`, `kept_movement`, `phase`, original finite storage/service envelopes and an explicit inside→inside `movement_area_route`. Hubble's native-input implementation consumes this proof for position-based initial cohorts, generation readiness, and input/movement/arrival ledger routes. **This validator alone does not implement those operations.** It deliberately leaves link21's whole-link detector assignment unchanged until the per-record projection hook can install an exact two-stock partition before initial state construction.

The old finite storage envelope114.9veh and through service413.061224veh/h are retained. The latter is the current calibrated per-lane scale206.530612 multiplied by two lanes; the two-lane geometry is verified, but neither value is claimed to be an independently identified source21 capacity. No new saturation, jam spacing or fitted parameter is introduced. The existing downstream SC109 routing is retained as a modeling approximation: routing decision1081 is on174 at11.922704m, before this cohort merges at75.408322m, so its407:1304 weights cannot be promoted as source21's eligible route prior.

Focused tests: `python -X utf8 -m unittest diagnostics.test_native1083_signal_authority -v` —6PASS. They cover exact OFF no-op, physical proof/clock/stale-phase rejection before commit, other-source/gate rejection, unchanged retained capacities and unrelated routing, one retained beta1 movement, and actual900 pre1/post2 conservation. Existing physical-route and three-phase-authority suites —16PASS. Actual1083 generation, per-record projection, red/green release, receiving rejection and complete ledger conservation remain integration checks owned with the native-input helper; these are not claimed by the six source-contract tests.

Integration-test cleanup: the fixture initially leaked `RW_OFFSET_WRITER=experiment` into later tests. It now scopes that environment setting to the clock configure call and restores it immediately. The requested same-process order `test_native_demand_forecast`, `test_native1083_signal_authority`, `test_observation_projection` passes all22tests in9.859s. The earlier failures were test environment contamination; no production change was needed for that issue.

Owned changes: the one new function in `evaluation/controllers/physical_movement_routes.py`, the pinned1083 JSON, `diagnostics/test_native1083_signal_authority.py`, and this handoff. No adapter, runtime initializer, active config, VBS, network or `.sig` was edited for this subtask.

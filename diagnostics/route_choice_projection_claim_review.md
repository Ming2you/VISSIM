`projection_support.configure` now accepts only a verified route-corridor physical-stock partition. A marker by itself cannot suppress the positive-unresolved guard. The guard checks the configurator's network and raw evidence SHA, validated-path proof, exact evidence/config partition, exact claim table, real target storages, singleton origin and transit markers, absent movement projection, local/full record counts, and no freeway/ramp ownership overlap. A support-table overlap is allowed only when both mechanisms name the same storage. An unclaimed positive link still fails explicitly.

The new module `route_choice_corridor.configure` remains responsible for validating the native physical paths before issuing the proof. The support guard then verifies that `prepare_projection` produced that exact partition for this snapshot. The guard does not redraw route choices, create a source, reassign Ω membership or generate initial crossing events. When both the corridor spec and marker are absent, its return is empty and the original support behavior and metadata remain unchanged. The physical-support flag's OFF path returns the same input objects.

`python -X utf8 -m unittest diagnostics.test_route_choice_projection_claim diagnostics.test_projection_support_road_paths diagnostics.test_area_projection_coverage -v` passed17 tests in7.871 seconds. The five new tests use the actual1350 raw records with nothing removed. They prove10634's three vehicles are assigned once to `storage:SC1004_E_choice`, while336's vehicle is assigned once to `storage:SC101_to_SC5`. The freshly seeded Ω ledger equals the actual physical inventory and starts with entered=TTD=event_count=0. Fake marker/config targets, source fingerprint changes, conflicting origins/movement projection, count discrepancies, reserved-channel overlaps, and a positive10379 outside the claim all fail. The raw source bytes are unchanged.

This validates the projection/initialization contract, not the complete dynamic route-choice engine, which has separate tests and ownership. It does not authorize a single-receiver alias for10634 or alter the unresolved-support source JSON. All28 unresolved data entries remain recorded; the eight physically verified corridor stock links are admitted conditionally only with the configured model and its matching projection.

For the next four positive gaps, `positive_unresolved_connector_review.json` provides a proposed initial-support contract and a bounded1128 reuse design.10379 can be justified from the exact three SC11 accepted upstream receivers.10381 requires an explicit native1091-to-model-origin declaration because the existing input gate map is empty. A regression for either eventual addition should use a complete copied real record at the connector, verify one storage assignment and initial entry/exit=0, corrupt each source/native/receiver identity to verify rejection, check mixed downstream-road assignments remain unchanged, and advance the accepted transit reservation through the canonical endpoint. Neither positive10610 nor10611 can be repaired by a single destination alias before native1128.

# 전달 검증

2026-09-19, 원래 작업 경로 밖의 `.handoff_validation_20260919`에 Git 추적 파일과 이번 명시적 전달 파일만 복사하고 두 패키지를 복원했다. 원시 FZP와 로컬 dependency/cache 폴더는 복사하지 않았다. VISSIM 실행과 프로세스 조작은 없다.

| 검사 | 결과 |
|---|---|
| 기존9월16일 패키지 복원 및 SHA | 2255개, 누락0, PASS |
| 이번 증분 패키지 복원 및 SHA | 7560개, 누락0, PASS |
| 직접 전달 소스·문서·분할파일 pin | PASS |
| 최종 모델 실행 소스 freeze pin | PASS |
| 단위 검사 | 65개 PASS(모델56개+복원9개) |
| 복원 경로의 seed33,2400–2850초 무제어 예측 | 기존 셀·유량·진입/진출 기록 전체 정확히 일치 |
| 내부 체류 비용 | 본선152.3379883552 + 진입11.5624365181 + 진출8.7645151596 =172.6649400328veh·h, 기존 저장값 일치 |
| 선택 파일 수·Git 크기 | 명시적114개, 개별50MiB 미만, 원시 FZP/DB/knr/rsr 없음 |

이 비용은 본선42셀+진입8개+진출8개 connector의 component 범위이며 Ω 전체 비용이 아니다. 기존 기하와 달라 재검증이 필요한 `leader_hinge`, `np_deadband`, `leader_mfd_far` 경고는 그대로 나타난다. 이것을 full GNE 또는 새 폐루프 성능 검증으로 간주하지 않는다.

증분 ZIP은4075개 고유 객체를4개 분할파일(총184.03MiB)에 보존한다. `verify_portable.py`로 위 단위 검사·pin·예측 비교를 다시 실행할 수 있다. 분석 및 결과 파일을 덮어쓰지 않는다.

# Installed physical head resource validation

The canonical helper is now tested through the actual runtime, main builder, urban method, local consumer and serialized worker. No proposal module, generated method body or module substitution is used. The baseline observer spy only captures its unmodified arguments for raw-observer and OFF comparisons. Production files were not edited by this migration; no endpoint, optimizer, FZP scan or VISSIM was run.

`python -X utf8 -m diagnostics.verify_head_service_resources_portable` passes 16 tests in 23.124 s (25.251 s including startup). Source hashes are unchanged. Original evaluation/runs reads/listing and Git subprocess calls are denied; both attempt counters are zero. The fresh worker receives private serialized inputs only. The unchanged fixture v2 archive preserves the complete original windows and provenance; large FZP data is not needed.

| Window end | Observed-only 10619 support (veh/h) | Observed-only 10629 support (veh/h) | Final 10629 resource |
|---|---:|---:|---|
|150|0|0|Not activated; inherited views unchanged|
|300|0|0|Not activated; inherited views unchanged|
|450|784.6153846|0|Not activated; inherited views unchanged|
|600|784.6153846|1040|One shared 1040 budget|
|750|784.6153846|1040|One shared 1040 budget|
|900|784.6153846|1040|One shared 1040 budget|
|1050|784.6153846|1040|One shared 1040 budget|

Each row comes from a new actual configure_runtime call. A private copy of the corresponding action carries only the metadata generated by that call into the next window. The canonical contract hash is d275ce27e0b11e5ab53f157dc6ec3c15365a07e3138361238caf2dfd02121ad9; the previous proposal namespace is not reused. The archived v2 configuration plus this one contract key equals the actual v4 beta0 configuration as a complete dictionary (separate config-equivalence JSON).

10619's support is below its existing 826.122449 rate, so it is unchanged. The legacy seed-max 1445.714286 is not promoted to observed support. The pre-head movements retain their original seed values, nonexistent aliases remain removed, and the independent 10634 resource stays unchanged. Final 10629 cap-map entries are views of one 1040 resource, not two additive capacities.

The actual 1050 main builder local score is 16.203408254098534, repeated exactly and returned exactly by a fresh pickled controller worker. Both carry the same start_step=210 ready seed and groups10629/10634. Separate full/partial/zero-GREEN and blocked-receiver tests verify accepted-only single-budget consumption, candidate/substep independence and local receiving. One canonical urban 5-second call reproduces exactly on an independent copy while leaving its input state unchanged. These checks establish service wiring and conservative ownership, not full traffic-prediction accuracy or saturation capacity.

## Cold-start and OFF scopes

Actual state1 configure_runtime with the CLI's default previous='' succeeds with zero new support. Direct head.observe(previous=None) also succeeds. The outer configure_runtime/adapter path has no Optional declaration or None-normalization contract; its existing Path(previous_path) fails before the helper for None in both head-OFF and head-ON. Tests preserve that existing unsupported-input behavior instead of widening production APIs. The first overly broad test failure is retained in *_first_red JSON/log and head_service_resources_none_api_limitation.json.

OFF equality compares the canonical original observer result and complete serialized cfg with the head wrapper absent, plus no-op resource operations around the actual urban method and baseline physical caps. The earlier historical proposal-only AST comparison remains historical evidence; it is not rerun or reported as a canonical baseline implementation comparison.

## Outputs

- head_service_resources_canonical_validation.json: per-window observations/final groups, source hashes, cold-start and actual main/worker results.
- head_service_resources_canonical_portable_validation.json and canonical_console.log: 16-test result and archive SHA.
- head_service_resources_canonical_config_equivalence.json: private configuration exactly matches v4 beta0.
- combined_physical_contract_review.md: pending known-W_out overlap, ownership and later integration tests.

Historical head_service_resources_validation.json and head_service_resources_portable_validation.json are unchanged. Root owns staging, live-run decisions and any later production changes. Known-W_out is not included in this head-only canonical pass.

At1350 seconds in `codex_area_beta0_retry_s13_20260910`, vehicle6093 is1.12658m along road336 at43.85246km/h. It is an observed Ω vehicle with no existing model observation target. A deterministic physical receiving path can be proved for336; the other13 unresolved roads cannot be assigned using the same evidence.

The full local route is `1220011503 →10528→336→10529→1210014303`. Road336 has one incoming connector10528, one outgoing connector10529, no signal head and no vehicle input. Connector10528 enters336 at0.651921m;10529 leaves at14.506713m. The14.7722m road can therefore be traversed forward without any intervening choice. The terminal road1210014303 supports the existing storage `SC101_to_SC5`. Current canonical movement `SC101_S_SC1_to_E_SC5` uses that same receiving storage and explicitly records `[336,10529,1210014303]` as its destination support path. Native route1014:3 contains the complete source-turn-path sequence.

This is a **branch before the head on the immediately preceding road**. Connector10528 leaves source lane1 at193.004715m; that lane's SC101 head is at209.187548m. The336 road body has no head. This local geometry alone does not establish whether an earlier road applies signal control; full upstream authority is audited separately. An initial vehicle already on336 has left the source approach and belongs in the receiving transit stock. Restoring that stock does not establish or change the model's signal-service assumption, greens, service rates or turn probabilities.

The optional `downstream_path` evidence accepted by `projection_support.configure` validates the pinned network and contract, exact ordered road/connector path, no signal/input on its interior, one outgoing connector per interior road, forward positions, all incoming approaches covered by the accepted movement, matching active-model receiver, actual terminal storage support and an exact native source-turn-path sequence. It rejects conflicting active receivers. The existing ordinary connector support and disabled paths are unchanged. This is explicit evidence supplied in reviewed data, not an automatic nearest-road traversal or a default for unknown links.

`test_projection_support_road_paths.py` passed7 tests in2.020 seconds. The actual1350 snapshot is copied and the three unresolved10634 records are explicitly removed **only in that isolated diagnostic fixture**; the original snapshot is never changed. This allows336's independent contract to be tested while the shared10634 route architecture remains unresolved. One vehicle is assigned only to `storage:SC101_to_SC5`, no movement duplicate is created, initial entry/exit/event counts remain0, the observed Ω inventory equals the initialized inventory, and the recovered target participates in the paired transit release buffer. A150-second installed endpoint preserves every modeled stock/ledger balance and leaves the initial state unchanged. Tests also reject disconnected/branched paths, signalled roads, terminal aliases, wrong contract hashes and unrelated native routes, and verify fixed old-data/OFF equivalence.

The isolated endpoint's aggregate prediction is not a full1350-scene validation or a VISSIM treatment result. The three10634 vehicles still require a shared pre-choice stock and physical route handling; they cannot all be assigned to SC107 or SC1005. `road_support_336_regression.json` identifies each removed diagnostic record and the raw snapshot SHA. After the initial temporary-data tests,336 alone was approved and added to production support data:187 additional supports and28 unresolved links. The support SHA changed from `366e6c4c99e0ed311aef4fb7eb718d6adfb62edad71772d1440f113d9607c174` to `4d5901d69f386211236a6981695794eee38e9b26762b15e8da48dcfdd22d980e`. `road_support_336_data_update.json` preserves both revisions and confirms no10634 alias was added.

The road audit is retained in `unresolved_road_support_review_1350.json` with network, snapshot, config and contract SHA values. The13 residual road cases remain explicit:

| Road(s) | Why the single-receiver contract is insufficient |
|---|---|
| 141,197,224,360,459 | Two physical exits lead to distinct opposite-direction/model origins. |
| 187,189,209,240,242,251 | A physical signal and multiple exits require the road's own service/route treatment. |
| 247,249 | The physical exit is single, but internal origin and external `in_SC*` cohorts share the corridor; no canonical accepted-movement identity fixes a single receiver. |

These residual cases are not assigned equal shares, mapped to zero or silently merged. Positive observations still produce an explicit unsupported-link failure. Their source/input/route history must be represented before they can be included in a complete future scenario.

# 1단계 — 컨트롤러 테스트 재실행 (실측)

실행 환경 (재현 필수):
  PYTHONPATH = <repo>/.review-deps/sdmpc ; <repo>/.review-deps/sdmpc-numba   (워크트리 아님, 저장소 루트)
  PYTHONUTF8 = 1            # 한글 포함 UTF-8 JSON 을 cp949 로 읽어 깨지는 것 방지
  RW_OFFSET_WRITER = experiment   # prepare_area_candidate_configs.py 가 required_process_environment 로 선언
  OMP/OPENBLAS/MKL_NUM_THREADS = 1
  대상: C:\Users\TRLAB\Documents\ChatGPT\VISSIM\.worktrees\sdmpc-tangent-20260921 (미커밋 상태 그대로)

## 결과

| 검사 | 결과 |
|---|---|
| diagnostics.test_sdmpc_pfo_caps | 12/12 PASS |
| 인계 지정 10개 suite 합계 | 111/111 PASS |
| scripts/verify_parameters.py config_candidate.json | PASS |
| tests/ 전체 (pytest, 218 collected) | 59 failed / 162 passed / 5 skipped / 26 errors |

## 판정

인계 #1 의 "PFO 테스트 12개 중 4개 fixture lambda pickle 오류, 수정했으나 재실행 못 함,
수정 후 통과로 기록하면 안 된다" → **직접 재실행하여 12/12 PASS 확인**. 이제 기록해도 된다.

tests/ 의 59 failed + 26 errors 는 회귀가 아니다. 동일 환경에서 5fdfabd(컨트롤러 미커밋 작업
제외) 기준선을 돌린 결과가 `59 failed, 162 passed, 5 skipped, 26 errors` 로 **수치·실패 테스트
목록 모두 완전히 동일**했다. 회귀 0건, 개선 0건.

원인은 기존 저장소 부채다. FileNotFoundError 32건이 찾는
`evaluation/configs/real_world_modi_pstack_*.json` 는 base·5fdfabd·00abbab **어디에도 커밋된 적이
없다**. 즉 테스트가 미커밋 로컬 config 를 참조한다.

## 111 vs 이전 로그 106 차이

이전 tests_v2.log 는 67개, cap_tests_v1.log 는 51개로 부분 suite 였다. 이번 111 은 10개 suite
합계이며, test_sdmpc_sequence.SavedCoordinateTests.setUpClass 가 RW_OFFSET_WRITER 미설정으로
에러나면 그 안 5개가 실행조차 안 된다(106). env 를 세우면 111 이 된다.

## 남은 주의

test_sdmpc_sequence.SavedCoordinateTests 는 RW_OFFSET_WRITER 를 스스로 세우지 않는다.
offramp_ready_contract_review.py 는 patch.dict 로 세운다. 테스트 위생 결함이며, env 누출로
조용히 오염되는 사고(과거 RW_MAINLINE_SG_ONLY 사례)와 같은 계열이다. 차후 수정 후보.

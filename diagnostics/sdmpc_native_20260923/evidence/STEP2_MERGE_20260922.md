# 2단계 — plant(00abbab) + controller 병합 (완료)

결과물: D:\VISSIM-merge\sim3  브랜치 sim/merge-v2  커밋 2b46b80 (부모 96ea2a5 + 00abbab)

## 충돌 5건과 해소

| 파일 | 성격 | 해소 |
|---|---|---|
| area_freeway_accounting.py | 줄바꿈 아티팩트 | 3측 LF 정규화 후 3-way → 충돌 0 |
| control_area_objective.py | 줄바꿈 아티팩트 | 동상 |
| vissim_stackelberg_adapter.py | 줄바꿈 아티팩트 | 동상 |
| physical_lane_groups.py | **진짜 충돌** | 아래 참조 |
| AGENTS.md | **진짜 충돌** | union, PLANT 의 FW80/urban90 기록 보존 |

3측 줄바꿈 실태(이게 원인):
  area_freeway_accounting.py : base/plant/ctrl 전부 CRLF+LF 혼합
  physical_lane_groups.py    : 3측 전부 순수 LF  → 그래서 진짜 충돌
  AGENTS.md                  : base/ctrl 혼합, plant 순수 CRLF

## physical_lane_groups.py 상세

hunk1 시그니처 — 양측 union 필수. 한쪽만 택하면 반대쪽 인자가 **unused 로 삼켜져
NameError(line 1119) 또는 plant 기능 무력화.
  complete_allocator_scope=True,                                  <- CTRL
  ramp_entry_speed_kmh=None, junction_ramp_speeds=None, offramp_start_state=None, **unused   <- PLANT

hunk2 셀간 수송 루프 — CTRL 의 if/else 구조 + else 분기에 PLANT 의 junction_moment 누적.
추가로 array 분기에 가드를 넣었다:

  if scope ... array_transport:
      if junction_moment is not None:
          raise ValueError('array_transport does not accumulate junction_moment; ...')

이유: array 경로(sdmpc_tangent_transport.longitudinal → numba _transport)는 incoming 을
채우지만 junction_moment 에 본선 기여를 넣지 않는다. 그대로 두면
junction_mean_speed = ramp_in*ramp_speed/(ramp_in+mainline_in) 로 **가상 유입구 속도가
조용히 붕괴**한다. 예외도 안 나고 테스트도 통과한다. 그래서 조용히 틀리는 대신 막았다.
근본 수정(커널에 junction_moment 출력 추가)은 3단계 과제.

주의: lane_momentum_advection=True 면 직후 up 이 self.v 로 덮여 증상이 사라진다.
기본값 False 라 현재는 실재하는 결함이고, momentum advection 켜고 검증하면 "버그 없음"
으로 오판하게 된다.

## 검증

  충돌 마커 잔존           0
  compileall evaluation/   통과
  tests/                   176 passed (기준선 162, +14 = PLANT 신규 2파일)
  실패 집합                기준선과 완전 동일 → 회귀 0건
  PLANT 신규 테스트         14 passed (병합 전엔 충돌로 수집조차 실패)
  SDMPC suite              111/111 PASS (병합 트리에서 재실행)

## 3단계로 넘기는 교차 결함 (병합은 깨끗하나 의미가 어긋남)

M1 metanet_delta_merge / freeway_lane_drop_phi
   PLANT canonical_harness.py:588-591 이 문헌모형에서 0.0 으로 강제한다("문헌 속도식은
   relaxation/convection/anticipation 이므로 기존 경험적 merge/drop 감속을 겹치지 마라").
   CTRL 은 area_freeway_accounting.py:160 에서 direction_params 로 읽기를 재소싱해
   PLANT 의 쓰기가 무시된다. 문헌모형에 경험항이 겹친다.

M2 rho_max 소실
   CTRL metanet_parameter_transfer.configure 가 freeway_segment_params 를 CELL_KEYS 로
   제한해 덮어쓰는데 CELL_KEYS 에 rho_max 가 없다. PLANT _literature_target 의
   `row.get('rho_max', net.rho_max)` 가 전역값으로 폴백 → VSL 유발 FD 잼밀도 안전검사가
   셀별에서 전역으로 퇴화. 셀 실제 잼밀도가 전역보다 낮은 곳에서 올라야 할 예외가 안 오른다.

M3 junction_moment 배열경로 (위 가드로 현재는 loud, 커널 지원 필요)

M4 configure_state_response 설치 순서
   PLANT 의 freeway_fd.configure_state_response 는 EAGER 검증자로 freeway_segment_params 가
   비면 raise 한다. CTRL 의 metanet_parameter_transfer 가 그걸 덮어쓰므로 설치 순서 의존.

M5 CTRL 의 control_area_objective +52줄이 무테스트
   indexed_coverage / immutable_audit / primal_audit 세 플래그를 참조하는 테스트가
   diagnostics 전체에 0건. 병합 트리의 test_control_area_objective.py 는 PLANT 판 그대로다.

M6 verify_parameters.py 의 사각
   검사 대상이 network/mpc/leader 뿐이라 CTRL 이 추가한 SDMPC 상수 26개를 전혀 보지 않는다.
   "파라미터 검사 PASS" 가 그것들에 대해선 아무 보증이 아니다. sdmpc.territory_path 는
   어느 계층도 검증하지 않는 유일한 키인데 하필 정본 파일 포인터다.

M7 LaneResolvedRampBoundary.admit_current 가 집계 카운터를 재동기화하지 않아
   cumulative_admitted_veh 등이 snapshot() 과 어긋난다(단일차로 판은 갱신함).

M8 PLANT 가 분수 시각(state.time_sec, native 0.1s 해상도)에 대한 loud precondition 을
   조용한 수치 경로로 바꿨다. lane_coupling.run_interval 을 거치지 않는 호출에서 발현.

## 별건: 502168d 유실 위험
codex/sdmpc-controller-20260920 은 고아 브랜치다. 소스는 ctrl 이 전부 상위집합으로 흡수했으나
(심볼 비교 확인), **신규 테스트 12개 약 1470줄은 어디에도 없다.** 버리면 그것만 사라진다.

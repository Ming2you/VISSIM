# Native no-control5400: three-arm measurement preparation

Prepared only; no simulation, model, adapter or source edit. The parent will supply the completed run directories. The earlier1050 fixed-command result remains short-window evidence and is not the verdict on the network variants.

Use the existing `scripts/measure_control_area.py` over the complete0–5400 native FZP, with `--fzp-only --end-sec 5400`. The original **635inside/601outside** spatial partition and terminal links **24/120** remain unchanged. The three analysis-only membership copies in `diagnostics/startup_gui_three_arm_area_v1/{arm}/membership_analysis_only.json` already bind this exact partition to the corresponding baseline/LCD/upstream network SHA; no new network or runtime membership is needed. All physical link geometry/topology was verified equal, apart from the permitted10635 lane-change distance.

The JSON companion pins these inputs and lists the exact CLI argv templates. Substitute the actual completed run directory and write to a fresh output directory. Its templates are not run commands until that directory is supplied.

| Output | Definition and validation |
|---|---|
| Full0–5400 | One canonical measurement per arm, beginning with the tool's empty-network assumption at0. |
| Six demand windows | 0–900,900–1800,1800–2700,2700–3600,3600–4500,4500–5400, derived from the full cumulative series at exact boundaries. |
| Matching short windows | Optionally retain750–900 and900–1050 for connection to the completed1050 experiment; they are secondary diagnostics. |
| TTT and stock | Native1-second trapezoidal stock residence; retain left/right quadrature and final N. Verify link residence sums and cadence. |
| TTD | Observed inside→outside events plus separately reported verified-terminal disappearance inference. Repeat legitimate exits remain counted. |
| Entries/losses | Observed entries, first appearances inside and unresolved inside disappearances remain separate. Check every second and each window's stock closure. |
| Run end | Final-frame vehicles remain censored. No reward for file termination and no COM/FZP phase mixing. Any unobserved tail must be explicit. |

All eight on-ramps and six off-ramps are internal transfers. Off-ramps10479→125 and10645→123 leave the original Ω and remain legitimate outward TTD. New upstream routing choices do not change this spatial definition. First appearances inside cannot all be called internal generation; desired schedules and actually admitted vehicles must remain separate.

Before interpreting differences, verify all three runs share controller=no-control throughout, seed13, SimPeriod5400, SimRes1, FZP1s, native SIG bytes, demand profile and all204 interval volume readbacks. Match continuous/ForceStepwise mode and the actual command reapplication schedule. Urban signals must remain native, while recorded VSL/meter commands match across arms. Neither fixed action900 nor the paused SC1004 intervention is part of this comparison.

Spatial collection should use the same FZP and saved native LSA/ERR evidence, with targeted ID extracts reused by the existing spatial producer:

- **71/10635:** arrival lanes, first lane-selection/change time, SG5 stopline passage versus connector passage, waiting positions and45-second deletion events. Separate69/70/126 arrival paths.
- **Early1135:** source46/52/66→10625/10629/10633→68, then10681→2;10772→121→10646→120; or10772→121→10773→123 outside. Compare admitted source cohorts and observed outlets, not only intended RelFlow. Preserve source69/1134 and late31/10703 as unchanged controls.
- **420:** distinguish127→10118→30→10686 arrivals from72→10683 and329→10693; track exits10659/10660→1220007200, native green exposure and stopped positions.
- **E8:** preserve physical order10643→10639→10682→10681; report stocks/speeds and transfer counts without route-based redefinition of Ω.

Capture complete ERR bytes after each run before another run overwrites native files. Match deletion time/ID/link to the corresponding disappearance with the sampling-phase tolerance; never remove that ID's earlier valid boundary exits. Report remaining native input backlog and final stock alongside TTD. Losses can shorten queues and TTT without improving completion.

The analysis has no dependency on the temporary8a/958 clock-source exchange and is not a timing benchmark. Native no-control removes optimized signal/meter actions from the comparison, but a single seed with changed route/lane choices still does not isolate a specific microscopic mechanism or guarantee a common random-number trajectory.

Read-only feasibility: native SG head-state bulk capture

Scope: no VISSIM/COM instance created, no model/native/FZP execution, no source/config patch. Current controlled native run remains frozen.

The NC2850 saved state contains235 physical heads referencing114 distinct(SC,SG) pairs across17 SCs. Per-SC observed SG counts: SC1=6, SC5=13, SC6=8, SC7=6, SC11=5, SC12=8, SC16=6, SC101=8, SC105=6, SC107=6, SC108=6, SC109=4, SC1001=8, SC1002=8, SC1003=4, SC1004=8, SC1005=4. Thus each reported324900 counter is114*2850, not57*2*2850. Two CaptureHeadSignalStates invocations per second produce5700 timer calls, but the NC Seal invocation reuses obsActual at the same SimSec. Current dynamic scalar reads are649800 total (SigState plus ContrByCOM).

Local API evidence: installed PTV Vissim 2020 COM Intro section6.2.4, printed pages31-33, documents GetMultiAttValues(one attribute) and GetMultipleAttributes(Array(...)) with explicit attribute-column order and zero-based returned arrays. COM Basic Commands.py line129 warns the first GetMultiAttValues column is a consecutive container index, not the object's No. The installed COM.chm index has explicit ISignalGroupCollection and ISignalGroupContainer GetMultiAttValues/GetMultipleAttributes documentation entries. No SG bulk call has been executed in this task; return-type/error equivalence still requires isolated post-run qualification.

Preferred later candidate: CachedSignalController(scNo).SGs.GetMultipleAttributes(Array("No","SigState","ContrByCOM")), once per SC that still has missing observed SG keys. Explicit No avoids assuming sparse SG numbers equal array row indexes. At NC's current cadence this would replace649800 scalar attribute invocations with48450 bulk invocations,92.54% fewer calls (13.41x), plus clock/count/shape guards. Two GetMultiAttValues calls per SC plus a verified No mapping would instead require96900 dynamic calls,85.09% fewer. These are call-count reductions, not measured wall-time predictions. Reported269.641s head.signal_capture is the directly addressable measured region;335.906s vehicle scanning and115.320s history are separate and unchanged.

Required semantic preservation: RunSingleStep -> ValidateRuntimeSignalPersistence -> CollectHeadObservation establishes current post-step/right-end state before new writes. RunControllerDecision/ApplyRuntimeSignals/ApplyRuntimeRampMeters then feed immediate write readbacks into obsActual. SealHeadSignalStates copies this current, potentially overwritten-by-write dictionary into obsHeld for the next interval. Bulk fill must preserve both observation and seal calls, the current SimSec reset, the existing235-head iteration/history, and already-recorded state/owner/valid tuples. Do not overwrite entries furnished by failed or successful immediate/persistence readbacks with later valid-looking bulk data. Do not reuse a bulk table across a controller write or assume ContrByCOM remains native. Keep all writer commands, immediate readbacks, persistence checks and their counters unchanged.

Small implementation boundary if later approved: leave ObservationSignalReadback as the original scalar readback entry point, factor only its pure normalization/storage logic into a helper accepting explicit observed owner, and let bulk head fill call that helper for missing keys. Validate complete required SG coverage, unique positive No keys, equal array shape, supported state strings, actual owner booleans/accepted current encodings, unchanged clock and container. Extra SG rows in an SC must not enter obsActual unless referenced by observed heads. If bulk is unsupported or malformed, preserve fail-closed behavior; a deliberate scalar fallback must not hide a failed write or heal prior invalid evidence.

Meaningful later qualification: no new network-wide scan. At paused fixed native times compare all114 normalized(SigState,ContrByCOM,valid) tuples against original individual reads, including a sparse-SG SC5 and native-to-COM changes at a decision boundary. Synthetic VBS collection tests cover reordered/sparse numbers, duplicate/missing rows, bad state/owner values, bulk error, and preservation of invalid existing entries. A short paired native run must retain exact warmup/head histories and physical action/readback traces, while actual individual-vs-bulk COM-call counts and separate capture wall times are measured. This is future work and does not delay or change current native CL.

Source hashes:
scripts/run_real_world_stackelberg_controller.vbs ce0541673888e00f0160c01aca882e131606a95a6bd9fac639897bf643dcab4e
C:/Program Files/PTV Vision/PTV Vissim 2020/Doc/Eng/Vissim 2020 - COM Intro.pdf 2cb0c737342eedf077212421c41b17558b7e012ffe72333f0313150de5246f6a
C:/Program Files/PTV Vision/PTV Vissim 2020/Exe/Vissim 2020 - COM.chm f92182daa330ed237961b9c499dc7d9db4c6cfba843dc3dc2bf698b94ad2699b

Completed recorded900 v2 audit (read only; no new model/native/FZP run)

All 213 ordered physical CSV rows equal the actual900 reference: 17 signals, 122 SG rows, 66 VSL rows, and 8 meters. The response is selected_best_observed with refreshed targets, but physical action is unchanged.

Whole120/output10: common prices61.027s; model setup2.604s; candidate42.865s. The game had39.545s remaining, so reserve30 left9.545s search. No logical search evaluation completed and no improvement was accepted. Final audit consumed24 evaluations on its first owner FW_W (63 announced candidates), then decision_deadline/final_check. Completion0/19 and maximum gap null are correct. Search prefetch did real work:4 endpoint calls; final prefetch22 completed endpoints; one batch crossed its work limit. Query totals:114 submitted,106 completed; owned workers closed.

Chosen first congested native policy: existing fidelity_v3, whole wall disabled with local candidate120/restoration45,max8 and lookahead8/reserve30. This permits roughly90s search plus30s final audit after common prices. It preserves150s simulated control timing but makes no realtime or GNE claim. Keep fidelity_v4 finite-time evidence separately. Reserve10 would leave29.545s search at the same entry time, an arithmetic counterfactual only; no repeat is requested. The earlier bounded v1 changed SC5 (9 signal/SG rows), not SC108.

Exact reuse already implemented: one price field per decision across leader candidates; reference-action response cache hit; cached command evidence (134/213 hits,79 writer checks); fast NP seeds use existing measured responses. Cross-decision field reuse is not exact because state/clock/forecast/anchor change. The smallest possible later endpoint reuse is target-only retargeting of physically identical actions, but this is not currently authorized by the full-action response contract: first prove complete physical operand independence and exact rebound action/response/command evidence. It would remove only a small number of endpoints, not the77 independent full-rank price probes. No code change proposed for the current frozen native run.

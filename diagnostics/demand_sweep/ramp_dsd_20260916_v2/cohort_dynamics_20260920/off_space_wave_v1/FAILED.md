The first extractor selected seed13's older2250s baseline from the control-pair protocol, while the requested observations came from its4500s extension. The final4500s coverage assertion stopped this attempt. No native simulation was run or changed. The corrected extractor uses each observation manifest's source_run and verifies the complete FZP SHA. Results belong to off_space_wave_v2; this folder preserves the failed preparation record.

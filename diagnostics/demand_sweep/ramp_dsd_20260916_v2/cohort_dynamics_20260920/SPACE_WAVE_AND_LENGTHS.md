# 10643 공간 전달·차량 길이 진단: 물리 누락 확인, 이득 보정 실패

2026-09-20. **후보 미채택, 이득 예측 NOT_QUALIFIED.** 아래 관측·실험은 모두 완료됐다. 재실행할 필요가 없다. 새 말단/속도 진단은 [TERMINAL_AND_SPEED_RESPONSE.md](TERMINAL_AND_SPEED_RESPONSE.md)를 따른다.

## 관측으로 확인한 것

`off_space_wave_v2`는 기존 무제어 seed13/23/33의900–4500초 FZP를 사용했다. 같은 차로의 인접한 두 차량이3초 이상 함께1km/h 미만으로 정지한 뒤, 인접 관계를 유지하며 차례로5km/h에 도달하는 사례를 추렸다. 출발 시차 중앙값은 세 시드 모두2초였다. 관측된 큐 길이 구간별로도 쌍별 중앙값은2초였으므로, 이 자료가 개별 차량의 출발지연이 큐 길이에 비례함을 입증하지는 않는다. 전체 대기열의 반응 시간은 여러 차량 사이에서 누적될 수 있다.1초 기록 해상도와 임계 속도 선택의 한계가 있다.

세 자료 모두 초별 정상 진입·진출을 기존 port 사건과 대조하고,30초 재고121개씩을 확인했다. 최초 `off_space_wave_v1`은 seed13의 옛2250초 bank 경로를 선택해 중단됐다. 실패 기록을 남기고 v2에서 실제 관측 manifest의4500초 source_run을 사용했다.

추가 native 관측은 `vehicle_lengths_native_v1/run_retry`다. 기존 seed23 무제어3000초 기록에 FZP LENGTH와 VEHTYPE 열만 추가했다. 원래10열 **11,788,963행이 정확히 일치**했고 실행 검증을 통과했다. 소요시간194.38초이며 새로운 독립 성능 표본이 아니다. 첫 `run`은 CScript 호스트 접근 거부로 시작하지 못했으며 로그를 보존했다.

1800–2850초10643 lane2의 정지 인접 차량에서:

|선행 차종|전면 간격 중앙값[m]|차체를 뺀 순 간격 중앙값[m]|
|---|---:|---:|
|승용차100|5.92|1.41|
|택시150|5.80|1.21|
|화물차200|11.67|1.46|

즉 약12m 간격의 상당 부분은 화물차 차체였다. 입구6m 안에 정지 차량이 있던396개 프레임에서 `L−6N`의 명목 빈 공간은 평균58.92m, 실제 차체 길이와 설정 정지 간격1.5m를 반영하면34.95m였다. 공간 과대평가가 확인됐지만 이것만으로 실제 수용량을 특정하거나 남은 공간이 모두 입구에서 사용 가능하다고 볼 수 없다.

## 기존450초 모델에서 한 검증

`off_spatial_supply_v1`은 삼각 FD의 유한 후방 전달을 진단용 수용 상한으로 구현했다. 파속3.25573m/s는 무제어 seed13의 정지 인접 쌍만으로 정했고 이득에 적합하지 않았다. 현재 위치를 저장 한계 내로 투영하되 차량은 모두 보존한다. 공간·과거 배수의 관계는 [Jin의 LTM 정식화](https://arxiv.org/html/1405.7080v1)를 참고했다. 내부 차로 변경이 있는 구간은 이1차원 조건부 검증에서 제외했다. 실제 도착량은 서비스의 하한 자료이므로 상한을 낮췄다는 이유만으로 정확도가 좋아졌다고 평가하지 않았다.

`off_space_response_v1`은 seed23,2400–2850초의 네 명령을 기존 harness에서 비교했다. 현재2400초 차체 길이·위치만 사용하고 미래 배수는 모델이 예측했다. 차종 구성은 현재 차로 평균으로 고정한 근사이며, 이 상태의 과거 차로 교환률이0인 경우에 한정한다.

|예측 ΔTTT[veh·h]|RM|VSL|동시|
|---|---:|---:|---:|
|기준 후보|−0.03817|+0.02351|−0.01463|
|현재 차체 길이 반영|−0.03802|+0.02769|−0.01034|
|유한 공간 전달만|−0.03823|+0.02758|−0.01062|
|두 항 함께|−0.03790|+0.02977|−0.00810|

실제 RM−0.55333, VSL−0.99111veh·h와의 차이를 줄이지 못했다. 모두 한 상태의 무제어 오차10% 보호 범위 안이지만12상태/미사용 조건의 qualification은 아니다. 이 누락이 실제라는 사실과 전체 이득 오차의 주원인이라는 주장은 구분한다.

core·기본 설정에는 넣지 않았다. 기본 예측4개 JSON 일치,16개 예측의 보존·서측 불변을 확인했다. 후속 `terminal_response_validation_v1.json`에서 소스/설정 pin과 공간 수용5개 단위검사, 완료된 native 관측 기록을 다시 확인했다. 원본 망·수요·회전·신호는 그대로다.

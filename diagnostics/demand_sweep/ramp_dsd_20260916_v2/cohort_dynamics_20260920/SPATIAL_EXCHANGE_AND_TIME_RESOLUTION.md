# 공간별 차로 이동 검증과 남은 수송·속도 전달 오류

**NOT_QUALIFIED, goal active.** 공간별 이동률 수정은 순이득 검증을 통과하지 못했다. 새 기능 `freeway.physical_partition_exchange`는 기본 비활성이다. 정본 기본 설정·원본 망·수요·액추에이터·vendor를 변경하지 않았다. 이번 native 실런은 없으며 기존 FZP를 분석했다. 모든 자체 계산은 종료했다.

직전 사용자 설명 응답은 기존 결과 확인에 그친 **no progress**였다. 이번에는 현재 소스·프로세스를 재확인한 뒤 core/harness 수정,12개450초 예측, native 보존·차량 통과 분석과 수치 반례 검사를 완료해 **progress**다. 외부 차단 상태는 없다.

## 수정과 결과

기존 `physical_lane_groups.py`에서10643 분기 전후에 별도의 과거 관측 차로 이동률을 적용했다. 각 위치의 donor stock·목적지 접근·수용 공간으로 이동량을 제한하고, 진출 목적지 표지는 분기 앞 이동량으로, 합류 origin 표지는 분기 뒤 이동량으로 옮긴다. 분기 앞 공간이 꽉 찼다는 이유로 분기 뒤의 독립 이동도 함께 막지 않는다. rates는2250–2400초 기록에서 계산했으며 미래 반응이나 이득에 맞춘 계수는 없다. 시점이 없거나 현재 cutoff와 다른 rate 입력은 거부한다.

`branch_flux_audit_v1/past_exchange.json`은 미래 정보를 포함하지 않는 입력이다. 과거 pre/post exposure·사건 합은 기존 전체 셀 이동률과 정확히 일치한다. 반면 분기 앞3·4차로→2차로 hazard는0.0032967/s로 전체 셀0.0131915/s의 약¼이다. 전체 셀 평균을 양쪽에 그대로 적용한 근사를 검증하기 위한 변경이었다.

seed23·2400–2850초·동측 본선+on/off connector 비용 ΔTTT[veh·h], 제어−무제어:

|조건|이전 분리v4|공간별 이동률|native|
|---|---:|---:|---:|
|RM|−0.015661|−0.014011|−0.553333|
|VSL|+0.072158|+0.079186|−0.981944|
|동시|+0.056642|+0.065473|−0.515000|

새 후보의 RM 구성은 본선−0.285524/on+0.269036/off+0.002477, VSL은 본선+0.093707/on+0.010012/off−0.024533다. 무제어 상태 점수도2.645091→3.134263으로 악화했다. 기존 실패 후보보다 나빠졌으며 formal12상태 guard나 fresh holdout를 통과한 것이 아니다. 전체Ω 비용/TTD 검증도 아니다.

## 새 보존 분해: 잘못된 횡방향 이동이 다른 오차를 가렸다

동일45개10초 출력 시각의10643 분기 앞3·4차로:

|450초 수량 / 평균 재고|native|이전 분리v4|공간별 이동률|
|---|---:|---:|---:|
|상류 유입|329|315.258|233.413|
|분기 앞→뒤 통과|335|277.043|223.587|
|차로 이동 순유입|+4|−34.692|−1.565|
|평균 차량 수|4.911|11.170|14.225|

횡방향 순유출 오차는 크게 줄었다. 하지만 앞→뒤 통과가 부족하고, 쌓인 재고가 상류 유입까지 제한했다. 단순히 과도한 상류 수요가 들어와 쌓였다고 설명할 수 없다. 기존의 잘못된 횡방향 유출이 종방향 수송 오차를 일부 상쇄했다. 이를 되돌리거나 다른 상수를 올려 총 오차만 맞추지 않는다.

`branch_flux_audit_v1/result.json`은 두 native 조건의6공간×600초 보존식을 각각3600회 검증했다. 평균 재고 오차는 시각 가중 경계 유량의 합으로 정확히 분해된다. 이번 새 예측도 같은 계산으로 대조했다: `partition_exchange_work_v1/verification.json`, `model_flux_none.json`, `model_flux_vsl.json`.

## 10초 구간 안의 실제 통과

`partition_exchange_work_v1/passage_audit.json`에서 기존 native NC 파일의2400–2850초를 직접 추적했다. 원본 full SHA는 앞선 완전 추출의 검증값을 재사용하고 크기·mtime 불변,451프레임의 분기 앞1353개 group stock 일치를 확인했다. 이번 짧은 재추출에서 전체 파일 hash를 새로 계산했다고 주장하지 않는다.

- 176.03m 구간을 차로 변경 없이3·4차로로 통과한270대 중97대는 같은10초 모델 구간 안에 진입·통과했다.
- 해당 차량들의 표본 통과시간 중앙값은6초다. 각 사례의 진입/진출1초 구간과 위치 이력을 저장했다.
- 45개 계산 구간 중40개에서 실제3·4차로 통과량이 그 구간 시작 재고보다 크다. 이 수량 비교에는 중간 차로 이동도 영향을 줄 수 있으므로, 위97대의 무차로변경 사례가 더 직접적인 반례다.

현재 모델은 시작 재고에서만 방출한 다음 이번 구간 유입을 더한다. 따라서 같은10초에 들어와 이 짧은 공간을 지난 이동을 표현할 수 없다. 그렇다고 저장량 제한 없이 즉시 차량을 건너뛰게 해서는 안 된다. 경계·여행시간·실제 actuator 순서를 유지하는 수치 적분이 필요하다.

제조한 일정100km/h·차로 이동0·속도 ODE 고정 사례에서 기존 core의 같은 수송식을10/1/0.5초로 적분했다.10초 뒤 분기 앞 재고의 해석해1.92743대에 대해 각각5.27806/2.01988/1.97248대, 절대 오차3.35063/0.09245/0.04505다. 차량 합11대와 매 단계100km/h 조건을 검증했다. `transport_convergence_v2.json`이 유효 결과다. 이는 수치 절단 오차의 반례이며, 교통 예측 또는 RM/VSL qualification은 아니다. 정본 시간 간격은 바꾸지 않았다.

처음 수치 검사는 읽기 전용 `T_f_sec`에 대입하여 실패했다. 수정 후 첫 완료본 `transport_convergence.json`도 scalar nu=0/큰tau만으로 실제 속도식을 고정했다고 잘못 가정해 무효화했다. 설치된 segment wrapper가 계수를 다시 적용해 속도가107.54km/h까지 변했다. 원본 소스·결과·실패를 보존하고, v2에서는 제조 사례에서만 속도 ODE를 명시적으로 고정하고 매 단계 속도를 검사했다. 이 방법을 실제 교통 예측에 사용하지 않는다.

## 다음 수정의 우선순위: 속도식의 실효 기하와 문맥

`partition_exchange_work_v1/speed_context_trace.json`은 canonical fixture 한 번의 호출을 추적한 결과다. 기존 단일 adapter의 `_patched_metanet_speed_update_kmh`가 armed segment의 `segment_length_km`로 전달 인자를 덮어쓴다.

|분기 공간|core가 전달한 길이|실제 원식이 받은 길이|segment 문맥|
|---|---:|---:|---|
|앞|0.176030km|0.526288km|armed=True, 사용 뒤 해제|
|뒤|0.350258km|0.350258km|armed=False|

현재 core는 한 group에서 `segment_vsl`을 한 번 호출한 뒤 앞/뒤 및 ODE 세부 단계를 여러 번 계산한다. wrapper가 첫 속도 호출에서 문맥을 소비하므로 첫 단계만 segment override를 받고 나머지는 global 값으로 돌아갈 수 있다. **이는 새 공간 분리 후보의 구현 결함이다.** 이를 고치지 않고 시간 간격이나 FD 계수를 보정하면 잘못된 실효식을 보정하게 된다. 일반 aggregate 모델 전체가 항상 이 오류를 가진다고 확대하지 않는다.

다음에는 기존 adapter/core 경로에서 명시적 공간 길이와 segment 계수를 각 속도 계산에 일관되게 전달하는 최소 수정을 먼저 검증한다. 원래 길이·계수의 우선순위는 기본 경로에서 유지한다. 앞/뒤·여러 ODE 단계의 실효 인자와 기본 경로 전체JSON 일치 검사를 추가한다. 이후 기록 상태450초에서 해당 수정만 비교하고, 그 다음 실제 짧은 공간의 수치 적분 수렴을 확인한다. 제어 갱신150초·명령표·미터 양자화·원점별 설정 수요·총 유입·대기 비용은 그대로 유지해야 한다.

RM_C10490의 합류 반응 누락은 별도 위치이므로 이번10643 수정만으로 해결됐다고 판정하지 않는다. component gain/구성 비용/NC guard를 통과한 뒤에만 fresh seed와 필요한 짧은 native 비교로 넘어간다.

## 실행 검증

- 신규 공간 이동 계약4개 포함, 관련 검사48개와 추가 cutoff 검사1개 PASS; 각 runner의 기존 도시27계약 PASS.
- 유효450초 예측12개: 새 후보4개, 새 기능 OFF·partition ON4개, 두 기능 OFF4개. 두 비활성 비교 모두 이전 전체 예측JSON과 정확히 일치(총8개).
- 도시 보존5412회, 램프/본선 인터페이스1080회, source pin55건 검증. 공간 재고 합 잔차는1e−7보다 작다. parameter 검사PASS이며 기존 override/calibration 경고는 유지한다.
- 수정 전 소스는 `partition_exchange_work_v1/source_before.json`에 보존했다. 최초 후보 실행의 runner는 `runner_on_v1.py.txt`로 보존하여 CLI의 OFF 재현 옵션 추가 뒤에도 원래 source pin을 해결한다.
- 별도 adapter·새 plant 복제·native 실행·commit/push 없음. 사용자의 프로세스는 건드리지 않았다.

# 실제 RM 추종 반응과 공간 수송 후속

**보정 NOT_QUALIFIED, goal active.** 이전 전달은 `7bb685c`다. 이후 저장된 native 자료의 원인 분석과 국소 자체 예측을 완료했다. 새로운 VISSIM 런·정본 core/기본 설정·망·수요 수정은 없다. 이번 전달은 `diagnostics/handoff_20260921_spatial/README.md`를 따른다. 아래30초 실험에 이어150초 검사를 완료했으며 마지막 절이 최신 판단이다.

## 첫 제동 회피와 전체 반응을 구분

`paired_native_interaction_v1`은 동일2400초 전역5,189대 중 초기 동측 본선657대를 비교한다.2400–2460초 동안43대의 물리 상태가 달라졌다. 새로 생성된 같은 숫자 ID를 동일 차량으로 취급하지 않는다. RM의 추가 관측은 앞선 원래9열11,790,482행 정확 일치/LDP 검증을 통과한 자료다.

차량18665는 NC2417초, RM2419초에 합류한다. NC에서17532의 첫 제동을 유발한 native target이지만 RM에서는11507·12035·13050의 target으로 기록된다. 처음 기다린2초가 제동을 완전히 없앤 것이 아니다. 예를 들어12035는 RM2433초에41.62km/h, NC89.20km/h다. 이 차량은 다른 차로와 다른 추종 대상을 거쳤다.

|18665를 native target으로 기록한 초기 본선 집단|NC|RM|
|---|---:|---:|
|서로 다른 추종 차량|3|3|
|target 기록 표본[차량·초]|42|42|
|그중 Brake 상태[차량·초]|14|21|
|해당 표본에서 음의1초 속도변화 절댓값 합[km/h]|71.22|92.71|

native target은 직전 simulation step의 기록이다. 제동 표본 수는 독립 제동 사건 수가 아니며, 마지막 행은 에너지·TTT나 독립적인 인과효과가 아니다. 전체 matched 집단의 누적 속도 차이는2430초까지 양수였다가2440초까지 음수로 바뀐다. 이를450초 순이득의 원인 분해로 사용하지 않는다. 단순 합류량/평균 합류속도만으로 영향 대상·지속시간을 설명할 수 없다는 증거다.

## 이전 공간 FD 검사를 넘어 실제 재고를 이동

`downstream_spatial_rollout_v1`은 port가 없는cell15–20에서30초를 자체 계산했다. 대조군은 같은 국소 구성의6개 셀이고, 대안은 차로 분리 및 차로당약100m 공간이다. 기존 정본 METANET 속도 함수를 그대로 호출하며 계수·수요를 적합하지 않았다. 이 작은 비교기는 전체42셀 정본 rollout이나 새 adapter가 아니다.

- 초기 차량 위치로 공간별 재고/속도를 만든다. 비어 있는 공간은 현재 원래 셀 평균속도를 사용한다.
- 유입은 직전30초 실제 경계 통과의 차로별 평균, 상류 속도는 현재14셀 값이다. 미수용 유입은 별도 대기 재고로 남긴다.
- 차로 이동률도 직전30초 이력이다. 미래 관측으로 상태·유입·차로 변경을 갱신하지 않는다.
- 작은 구간 사이에 실제 수용된 차량 수와 속도 모멘트를 함께 전달하는 대안도 추가했다. 이 경우 Euler 대류항을 다시 더하지 않는다. 기존 core의 `momentum_advection`과 같은 수송 원리지만 작은 공간에서의 국소 시험이다.

|30초 평균속도 RMSE[km/h]|6셀 집계|차로+100m|차로+100m·속도 보존 수송|그 수송+계수 재적합|
|---|---:|---:|---:|---:|
|NC2400|7.740|8.839|6.868|11.246|
|NC2550|16.508|13.508|13.897|16.077|
|NC2700|12.436|10.751|11.111|14.893|
|RM2550|4.962|7.301|7.038|6.266|
|RM2700|9.433|9.897|8.321|12.181|

RM2400은 초기 상태와 과거 경계가 같아 NC2400과 예측이 같다. 이 국소 영역에는 직접 움직이는 VSL/RM이 없으며,2550/2700에는 이미 제어 후 달라진 시작 상태를 사용한다. 따라서 위 표는 국소 회복 예측이며 같은 초기 상태의450초 제어 이득 검증이 아니다. NC 세 상태 개선은 다음 연결 후보의 근거지만, RM2550 악화 때문에 일괄 개선이나 qualification으로 판정하지 않는다.

## 짧은 오차 적합이 오히려 자체 예측을 악화

공간 해상도를 바꾸고 옛 계수를 유지하는 문제를 확인하기 위해 NC2280–2400초만으로 완화·압력항의 두 비음수 배율을 적합했다. 제어 비용이나 미래 제어 결과는 학습하지 않았다. 유효 표본은6셀720개,100m8,945개이고 표본 가중치는 전후에 관측된 재고의 최솟값이다. 비음수2변수 최소제곱의 경계/내부 해를 직접 비교하고 KKT 조건으로 검증했다.

- 기존 Euler 대류를 쓴 두 해상도 모두 완화·압력 배율0이 최적이었다. 유한한 τ/ν로 옮길 수 없어 기각했다. 작은 양수를 대신 넣지 않았다.
- 속도 보존 수송을 쓰면100m에서τ34.0699초,ν0이 나왔다. 조건부1초 식 오차는10.943→5.379km/h로 줄었지만, 위30초 예측은 대부분 악화됐다. 이 계수도 채택하지 않는다.
- 이는 이 짧은 자료와 고정 FD/수송식에서의 적합 결과다. 모든 METANET 구조가 불가능하거나 실제 anticipation이0이라는 증명이 아니다. 조건부1초 오차 감소만으로 계수를 승격하지 않는다.

## 검증·실패 보존

`local_interaction_spatial_validation_v1/result.json` PASS:21source pin, 최초24개 전체30초 trace 정확 재현, 계수 후보8개 정확 재현,4개 적합 KKT 재계산, 균일 수송180개 셀 검사, 경계 속도 모멘트 보존. 차량 보존·비음수·jam 공간 검사는 각 rollout에서 수행했다. 속도 보존 수송만의 추가8개 예측도 저장했다. 이번에 완료한 고유 국소 자체 예측은40개다.

첫 계수 검사에는 설치되지 않은scipy import 실패가 있었다. 새 의존성을 설치하지 않고2변수 문제를 기존numpy로 풀었다. 다음 실행은0완화 해에서 명시적으로 중단했으며, 최종v3는 해당 결과를 기각으로 저장하고 독립적인 다른 해상도도 검사했다. `downstream_spatial_calibration_v1/v2`에 실패와 수정 전 소스를 보존했다. 유효 결과는v3 및 `downstream_spatial_moment_calibration_v1`이다. 이전 source pin은 스냅샷으로 검증하며 덮어쓰지 않았다.

주요 core4개는 `7bb685c`와 바이트가 같다. 자체 세션9295/25215/70574/87075 및 짧은 분석·검증은 모두 종료했고 다른 프로세스에는 개입하지 않았다.

## 다음 작업

1. 계수0/τ34·ν0 후보는 승격하지 않는다. 미세 공간+속도 보존 수송에서 계수를 유지한 경우만 남은 국소 연결 후보로 취급한다.
2. 저장된1초 집계로 같은 초기 상태·동일 과거 경계의150초 자체 예측을 확인해 과도한 회복이 다시 나타나는지 본다. 이어 기존 정본 경계/액추에이터와 연결할 때도 총 차량·차로·길이·신호·비용을 보존해야 한다. 전역 격자를 무조건 교체하지 않는다.
3. 현재660대 수준의 코호트 추종 반응과 국소30초 성과를450초 RM/VSL/동시 순이득 성공으로 대체하지 않는다. 전체 구성 비용·유리/불리 RM 사례·다중 상태 NC 보호·자유류 불필요 제한 회피·미사용seed 검증·정본 통합은 모두 남아 있다. fullΩ/fullGNE도 별도다.

## 후속 완료: 150초 예측과 실제 상류 경계 진단

`downstream_spatial_horizon_v1`의12개 인과 예측은 기존30초 trace와 정확히 이어진다. 계수를 유지한 차로별100m·속도 모멘트 수송도150초에서는 개선이 일정하지 않았다. NC2700의 속도 RMSE는18.442→16.447km/h로 줄지만 RM2700은15.865→18.347로 늘었다. 짧은30초 개선만으로 정본 연결을 진행하지 않는다.

`downstream_boundary_probe_v1`에서는 실제 미래 상류 통과량만 주는12개와 속도까지 주는12개를 추가 계산했다. 이는 원인 분리용이며 인과 예측·제어 성능으로 세지 않는다. 내부 상태와 말단 방출은 끝까지 자체 계산했고 모든 경우 경계 대기는0이라 대기 차량의 속도 보존 미구현 문제는 발생하지 않았다. 기존 인과12개 전체 trace도 정확히 재현했다.

차로별100m 모형의 RM−NC 국소150초 ΔTTT[veh·h]는 실제 경계를 주면2400초 −.217778(실제−.218611)로 맞지만,2550초 −.224831(실제−.043611),2700초 −.008660(실제−.656667)으로 어긋난다. 상류 통과량만 정확히 예측하면 해결되는 상태와 내부 전파·회복이 남는 상태를 구분해야 한다. 이는 미래 경계를 사용한 진단이므로 첫 구간 일치를 보정 성공으로 주장하지 않는다.

150초 비교는 port가 없는15–20셀뿐이며 Ω나 전체 동측 component 비용이 아니다.2550/2700초에는 제어 후 서로 다른 초기 상태를 사용한다. 다음 검토는2700초 이후 내부 반응과 실제 이득/손해가 모두 있는 RM 강도 대비다. 중간 강도 신규 native 실험은 아직 실행하지 않았다. 푸시 전 저장 결과의 핀·보존식·비용을 읽기 전용으로 재검사하며, 복원 후 검사 명령은 최신 전달 README에 있다.

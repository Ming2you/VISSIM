# 10634: the missing observation belongs to a shared route-choice corridor

The 1350 failure is a legitimate guard on an incomplete model projection. Assigning all three vehicles to either existing receiver would repair the count while selecting the wrong physical path for some vehicles. The smallest defensible repair is one explicit finite corridor before native decision1129, followed by destination-tagged travel cohorts. No production source, selected config, support data, or running process was changed in this audit.

## Direct evidence

Source: `projection_10634_evidence.json`, generated by `probe_10634_projection.py` using actual beta0 retry1350 records, earlier snapshots, the canonical Ver2 XML, and actual shared-runtime configuration from the valid1200 snapshot. The bounded FZP read covers1050–1350 only:53,971,723 bytes of154,804,497;1,036,164 selected rows. Range and small-source hashes are included; pinned sources did not change during the probe. There is no future trajectory after the failed1350 decision.

|Vehicle|1350 physical observation|Proven earlier source|SC1004 crossing|
|---|---|---|---|
|6119|10634 lane2,24.408m,49.686km/h|FW_W→10638→70→126→10641→71|entered10634 at1348s|
|6540|10634 lane3,18.291m,36.281km/h|FW_E→10643→10641→71|entered10634 at1349s|
|6788|10634 lane1,4.803m,32.911km/h|FW_W→10638→70→126→10641→71|entered10634 at1350s|

10634 is a64.204m,3-lane connector, from71@81.238m to56@2.233m. Signal heads on71 lanes1–3 are SG1004:2 at78.87–78.98m, upstream of the connector. These are post-stopline vehicles; returning them to `in_SC1004_W` would make them wait for and cross that signal again.

Native1129 is on road56 at253.172200720m. The incoming1126/1138/1140 route2 destinations end on56 at43.427m; incoming1125 route3 ends at42.448m. The next decision is therefore ahead of these reviewed entries, not behind them. Native1129 has:

|Native route|Physical continuation|Raw relative flow|Conditional prediction share|Existing destination|
|---|---|---|---|---|
|1129:1|56→10621→387|8|0.8|`SC1004_to_SC107`, bypasses SC1005|
|1129:2|56→10617→57→10624→61|empty, official default1|0.1|SC1005 through movement, then`SC1005_to_SC107`|
|1129:3|56→10617→57→10623→404|empty, official default1|0.1|SC1005 left movement, then`SC1005_to_SC105`|

Empty RelFlow default1 is supported by the matching installed VISSIM2020 `attribute.xlsx`, Attributes row1777, SHA139705be4bac9ac41e88c9c201a8704fb37f0631834dd49d546f57aa37e8c57e. These are conditional future-route probabilities, not recovered individual route IDs.

Road56 is469.968m,4 lanes. The bypass connector10621 starts at56@465.767m on lanes3–4. Connector10617 starts at56@467.344m on lanes1–2 and reaches57@2.096m. Road57 has the SC1005 signal heads at413.66–413.79m. Lane on the upstream portion of56 cannot alone prove the final path because lane changing remains possible before these diverges. Road57 proves the local rather than bypass branch, but not through versus left by itself.

All of10634,56,10617,57,10621,61,10623,404,387 are **inside** the fixed635-link Ω ledger. The earlier verbal description of404 as external was incorrect. No TTD belongs at10634, at the1129 branch split, or at404 entry. Any later Ω exit must use its actual downstream boundary contract.

## Why existing aliases cannot supply this state

The actual configured1200 detectors give10634 two aliases: `SC1004_to_SC1005` and `in_SC1004_W`. Road56 is wholly assigned to`SC1004_to_SC1005`, including two SC1005 downstream movement projections.10621 is assigned to`SC1004_to_SC107`. Thus the old alias on56 already selects the local path for traffic still upstream of1129. `dynamic_area_routes_ver2.json:33` and `area_dynamic_routes.extend_routes` prove the SC107 bypass is physically possible; they do not prove it is the only path on the shared prefix.

The current actual SC1004 east movements already exist in pairs, one receiving`SC1004_to_SC1005`, one receiving`SC1004_to_SC107`, with equal beta within each source pair. Source total beta is0.6 for offE/offW,0.458333 for W,0.296356 for N,0.291667 for S. The dynamic NC13 calibration concerns downstream SC107/SC1004 exit choices and phantom removal; it does not resolve native1129's initial shared-prefix vehicles. Applying that downstream prior here would route twice.

Do not sum the two existing service capacities when coalescing these pairs. Actual1200 per-movement capacities are206.531veh/h each for N/S,413.061 each for offE/offW, and619.592/413.061 for W. Their provenance must be reconciled to the single physical movement/SG/lane support; they are not two independent stop lines. Redirecting two independently served movements into a common receiver alone preserves this service ambiguity.

## Exact initialization scope

At actual1350 the shared physical prefix has **four** vehicles: the three10634 records and7428 on56@55.870m.10627 and10632 are empty. All four are before1129. The three formerly unsupported records add exactly3 physical vehicles;7428 is moved from its old assignment, not added again. Seed four inside vehicles into one corridor stock before area ledger construction; initial TTT, TTD, and entered counters remain0.

Keep already downstream observations separate:1350 contains one vehicle4834 on57@51.219m, five on10621, six on387, and one on404. These are already counted and must not be recreated by the new corridor. In particular4834 has selected the local path but the old record lacks its route2/3 identity.

This is not only a1350 corner case. Retry900 has one56 vehicle beyond253.172m.19 of the31 pure-n7 snapshots from900–5400 have1–5 such vehicles. Reinitializing those vehicles with a fresh8:1:1 draw is not a position-valid application of a future native decision. The evidence JSON lists every affected position and ID. Keep an explicit route-unknown cohort or collect its current route; do not silently label it eligible.

Installed VISSIM2020 officially exposes readonly Vehicle`RoutDecNo`, `RouteNo`, `RoutDecType` (Attributes rows1979–1981) and`VehRoutSta` (row1923). This permits a narrow extension of the paused full-record collector to preserve current1129:1/2/3 identities. Actual no-route sentinels and combined-route behavior still need COM validation. This audit did not call COM. At positions after253.172m, a valid current1129 route can tag the cohort without a new draw. On10621 the physical link itself proves bypass even if the route has ended at505.084m. On57/10617 a missing current identity only narrows to routes2/3; it does not recover their individual choice.

## Minimal integration contract for root review

1. Add a configured route-choice corridor module and pinned XML evidence. Reuse the finite-bin and occupancy invariants of`shared_approach.py:157`/`:208`, and the reserve-only receipt contract in`sc2001_corridor.py:250`. Do not instantiate shared69's external native-input machinery: this corridor has **no new vehicle input**. Source identity is provenance; future choice at the eligible1129 gate uses native8:1:1, not NC13 SC2001 priors.
2. Physically partition the prefix10627/10634/10632 and56 from the existing receiver inventories. Their road/connector geometry contributes storage capacity exactly once. Keep57 and10617 on the local branch,10621 on bypass, and their downstream stores intact. Recompute capacity/remaining travel distance from each partition's actual geometry and the existing jam source, not aggregate constants or summed old capacities. For initial post-decision56 records require observed route identity or an explicitly reviewed conditional observation model; unchanged native8:1:1 is not automatically a valid posterior.
3. Coalesce the physical east turn per SC1004 source before raw projection. Preserve the sum of its two prior betas as the east-turn total, but use a single verified physical service capacity. Invalidate movement/spec/routing caches and update detector movement IDs and capacity/head references. All accepted east turns enter the same finite corridor, not already separated SC1005/SC107 reservoirs. Replace the corresponding full-path Ω movement contracts by the real stopline→prefix transfer; the future prefix branch event owns the later transfer.
4. Candidate-private state has undecided travel bins to1129, then route-tagged bins to10621 or10617/local branch. Native choice changes only a tag, never physical stock or ledger metrics. Blocked receiving retains the tagged vehicles and source stock. Initial per-record residual distance prevents restarting the entire corridor travel for a vehicle already halfway through it.
5. Extend canonical `_receive_corridor` at`urban_flow_accounting.py:16`, already called after actual accepted stock transfer at`:85` and`:420`, to dispatch this new receipt. As for SC2001, receipt schedules only its private travel bins and returns handled; it cannot add stock or ledger events. Exclude the new stock from generic storage-release, arrival, and sink paths. Advance once at the same explicit urban step before normal releases. Require stock occupancy==sum(private cohorts) before/after.
6. At actual branch reception use current `_effective_available_space` and accepted transfer, reducing source exactly once. Bypass enters`SC1004_to_SC107` and uses that remaining-path arrival/release pair once. Local branches retain route2/3 through57 and target the corresponding SC1005 movement queues, so generic source beta0.9/0.1 does not re-split them. This needs a typed local arrival reservation; do not also create a generic`urban_arrival_buffer[SC1004_to_SC1005]` reservation for the same vehicles. Physical57 occupancy and point queues must share its one receiving capacity.
7. Install/configure before support validation and raw projection in`runtime_setup.py:117`; initialize after the conservative initial transit pairing at`:139` and before area seed at`:148`. Worker install restores hooks only; it must not initialize from raw again. Ensure candidate copy deep-copies corridor and typed local reservations. Ω OFF with the physics flag ON must still execute the same corridor; Ω accounting merely observes accepted moves.

The present10634 guard should be removed from the unresolved set only when this new module owns these physical records and confirms source/route completeness. Hubble owns`projection_support.py`; this audit proposes no changes to that file or its support table.

## Required meaningful verification before enabling

- Actual1350: three previously missing records restored,7428 moved once, total raw Ω equals model Ω; shared occupancy4; initial area events0. Confirm downstream57/10621/387/404 counts remain identical.
- Synthetic eligible N10: choice0.8/0.1/0.1 creates8/1/1 tagged mass, no entry/exit reward. Full bypass receiver retains only its8; local2 progress if their receiver accepts. Shared receiving space is recomputed sequentially for two concurrent sources, preventing double reservation.
- Initial56 positions253.172−epsilon and253.172+epsilon take different eligibility paths. Valid current1129:2 is preserved; missing/wrong route metadata after the decision fails explicitly or enters the separately approved unknown-route model. Lane alone is insufficient before the physical diverge.
- Typed local arrival1 for route2 reaches only the SC1005 through queue; route3 reaches only the left queue. No subsequent generic0.9/0.1 split, no duplicated shadow release, no sink TTD, even with a red SC1005 signal.
- Empty/fresh/copy candidates, main/repeated worker/fresh worker, physics ON/Ω OFF, and absent-flag golden comparison.450s actual current-action replay including1350 and the initial900 post-decision record; all stock and event closure checks remain strict.
- Separate service-capacity proof for the merged SC1004 movement. Passing count closure alone does not establish correct signal capacity or travel-time fidelity.

Status: diagnosis and implementation contract ready; no production implementation or post-fix performance result is claimed.

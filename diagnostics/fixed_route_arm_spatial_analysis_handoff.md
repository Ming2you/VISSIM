# Fixed route-arm spatial analysis — synthetic checks pass; no actual batch analyzed

This is a new diagnostic consumer of Flow's `fixed-beta300v3-three-arm-experiment/v1` completed batch. It does not run VISSIM, import the MPC adapter, or change any production/configuration source. Following the actual trace timeout, root authorized the three small synthetic guards: all pass (unittest0.020 s; import/runner included0.107 s). Actual FZP reads, COM calls and traffic-model imports remain0. Final evidence is `fixed_route_arm_spatial_validation.json`; prior AST-only preparation records remain historical.

Files: `analyze_fixed_route_arm_windows.py`, `test_fixed_route_arm_windows.py`, this note. The analyzer accepts `--batch-manifest diagnostics/fixed_beta300v3_experiments/<batch>/manifest.json --output diagnostics/fixed_route_arm_spatial_<new-name>`. Run as a module from the worktree with the canonical Python. The output directory must be new. Do not run until all three arms have passed the driver's execution/command/readback gate and root opens a quiet analysis interval.

The producer reuses `diagnose_sc1004_first_control_window.analyze`, `IndexedFzp`, the existing physical head-event logic, and native ERR classification. It calls the existing analyzer sequentially with scoped diagnostic globals, restoring them on exceptions. There are six bounded indexed reads: three arms × `[750,900]` and `[900,1050]`; each retains the original 64 MiB / 45-second limit. The second stage uses each selected gzip cache rather than scanning FZP again. It refuses multiple FZP/LSA replicas, changed lengths/timestamps, incomplete one-second grids, a changed cache, and changed analysis-input hashes. Selected-cache decompression has a 128 MiB character limit. Historical runtime SHA certificates are retained, without claiming current analysis code is the historical controller.

The two 150-second windows share their endpoint state, but not their sampled service interval. Warmup SG state comes from native LSA through `post_step900`; controlled state begins at `immediate900`. `post_step1050` closes the last bracket, and `immediate1050` is excluded. This reproduces the driver's declared cutover. The manifest's command, raw-state, readback and actual network paths/SHAs are checked before collection and checked for change afterward. An actuator certificate is evidence of command execution, not a certificate of realized route eligibility or performance.

| Question | Recorded evidence | Denominator / unresolved cases |
|---|---|---|
| 71→10635 discharge/blockage | SG2/SG5 exposure and qualified head crossings; 71 stock closure; observed connector departures; per-lane changes; near-head stopped GREEN samples; explicit ERR deletion and selected-route1126:1 tags | A one-second presence count is not capacity. Global FZP absence remains separate from recorded deletion. Near-head stopped time alone does not prove the cause of blocking. |
| Upstream1123/1124/1125 choices | Same-road gate brackets, lane pair and interpolated gate time; observed replacement stem10629/10633/10625; later outlet10646/10773/10681; current-route snapshots and outlet departures | Conditional outlet fractions use only observed replacement-stem cohorts with resolved outlets, with resolution coverage. Other gate branches are not in this denominator. Short skipped connectors, repeated crossings, vehicles first seen after the gate, and right censoring remain explicit. |
| Nine new route labels | Exact treatment XML path/RelFlow/destination checked against the nine-row static proof; STATIC current-route4/5/6 observations joined within the same vehicle/run | Raw `route_decision_type == 'STATIC'` is required, as in the canonical route helper. Choices are only observed at150-second envelope times. New IDs map to old1135 branches2/3/4 for a common conditional table; no missing route is reconstructed. |
| 420 platoon/SG relation | 420 arrivals with observed127/329/72 history; downstream1220007200 first sample; first stopped420 sample; SC105 SG4/7 GREEN at arrival | SG timing at420 is temporal context. Travel delay and actual downstream SG assignment are not inferred from that snapshot. |
| Unfinished / removal | Native explicit lane-change removals, ignored static routing and next-link warnings, remaining vehicle inputs | Ungenerated input remainder is not deletion of an FZP vehicle. A next-link warning does not itself say the vehicle was removed. |

All arms end at1050, but their networks differ from time0. The upstream route-tree expansion changes where/when random route draws occur; seed13 does not establish matched individual choices, states at900, or OD equivalence. Vehicle IDs are therefore joined only within an arm. The pre750–900 window is reported separately from the post900–1050 window; it is not proof of full0–900 equivalence. A treatment outcome is an observed sensitivity of the full routing process. This tool does not fit a capacity, lane-change success rate, FIFO parameter, or forecast correction.

The three synthetic guards cover conditional-stem denominators, STATIC type restriction/lane brackets, incomplete-batch rejection and duplicate commands. They also exercise the newly required baseline trajectory certificate: a false valid flag, inconsistent row counts, a missing equality-field set, and the wrong reference FZP are rejected. The analyzer requires the driver's complete ordered FZP/header/count/terminal1050 equality result before collection, checks reference/source identities and small provenance SHA pins, and retains the full certificate in its output. It does not independently rescan the complete baseline FZP or extend that baseline equality claim to the two modified-network arms.

A later completed-batch smoke is still required to validate actual XML/envelope/native-file schemas before citing numerical results. No actual comparison output exists yet. The preceding750–900 and controlled900–1050 cohorts retain separate denominators and right censoring. Their shared endpoint does not merge their arrival or departure events, and the observed gate cohort is not a native route-eligibility certificate.

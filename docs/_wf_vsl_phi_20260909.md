## 헤드라인

**h1 팔은 21:00 시작해 22:29:24 에 끝났다** (`WATCHDOG_PROGRESS.txt`, elapsed 5326s). 결과가 두 질문을 대부분 대신 답한다.

| 팔 | TTT (veh·h) | 비고 |
|---|---|---|
| no-control | 7403.9 | |
| f3 (분기점) | 7496.6 | |
| **h1 (landing seed)** | **7556.2** | **f3 대비 +59.6 / 무제어 대비 +152.3 (3.0σ)** |

`state_*.csv` 의 `total_vehicles` 사다리꼴 적분(브리프의 7403.9·7496.6 과 정확히 일치하는 관례). h1 과 f3 의 config 차이는 **`urban/ramp/landing_storage` 4개 블록 + name/description 뿐**(전수 diff). 즉 **+59.6 은 "off-ramp 차로폐쇄(λ_eff)+상류 φ 항을 무장한 것" 단독의 값이다. 병목을 만들어 준 팔이 졌다.**

---

## 먼저 정정 — 숫자를 바꾸는 세 가지

**(1) `lane_reduction` 은 0.35 가 아니라 1.0 이다.** lens vsl-authority claim 15("dataclass 기본값 0.35 가 산다", verifier STANDS)가 **틀렸다.** 어댑터는 `config_path = repo_root/"src/config/default.yaml"` (adapter:7513) 를 base 로 `ExperimentConfig.from_file` (adapter:7669) 을 부르고, `from_file` 은 `load_jsonish(path)` → `_deep_update` → `from_dict` (state.py:1090-1094), `from_dict` 은 `CapacityDropConfig(**raw.get("freeway_offramp_capacity_drop", {}))` (state.py:926) 이다. `default.yaml:187-193` 이 `lane_reduction: 1.0` 을 명시하고, 어떤 config 도 이 블록을 덮지 않는다(전수 grep). **따라서 Δλ 는 0.14~0.27 이 아니라 0.02~0.756 이다.** vsl-authority 의 λ_eff·용량손실·φ 크기 수치는 전부 2.86배 과소.

**(2) `vsl_set = [80.0, 100.0, 120.0]`** (`config_overrides.freeway_follower.vsl_set`). 브리프의 "60..120" 은 오류다. **80 이 바닥이다** — 이미 포화된 레버지 조절 가능한 레버가 아니다.

**(3) VSL 구역 1 은 셀 5–9 다. 80 캡이 병목 셀 8 "위"가 아니라 셀 8 "에" 걸린다.** `install_freeway_vsl_zones` 는 `for j,h in enumerate(hs): if h<=i: z=j` (adapter:8371-8382), 즉 *i 이하의 가장 큰 머리*. heads `[0,5,10,15]` → 셀 5–9 = 구역 1(머리 seg5). 실측으로도 31개 제어 결정 전부에서 `vsl.FW_E__seg5 == vsl.FW_E__seg8`. 다이버지(셀 8)와 머지(셀 9)가 같은 캡을 받는다.

---

# Q1 — VSL 에 여유가 생겼나

## 1. 셀 8 병목에 대한 실권한: **아니다(No).** 부분적도 아니다.

세 가지가 각각 독립적으로 충분하다.

**(a) FD 벽 — 용량 권한 정확히 0.** `vsl_fd_two_branch` 는 config·`default.yaml`·`state.py` 어디에도 없어 `getattr(...,False)` → VSL 은 순수 캡 `min(V(ρ), (1+α)·vsl)`, α=0 (metanet.py:96). 지수 FD 의 유량 최대는 어떤 a 에서도 ρ=ρ_crit 이므로, 캡 c 는 `c < V(ρ_crit) = v_free·e^(−1/a)` 일 때만 용량을 깎는다. 셀 8 (`FW_E_S8`: v_free 118.29, ρ_crit 25.0, a 2.0) → **V(ρ_crit) = 71.75 km/h.** 0.005 격자 탐색으로 `max_ρ ρ·min(V(ρ),c)·4`:

```
c=120 → 7174.7   c=100 → 7174.7   c=90 → 7174.7   c=80 → 7174.7   (비트 동일)
c=70  → 7170.4 (−0.06%)   c=65 → 7112.9 (−0.86%)   c=60 → 6990.8 (−2.56%)
```

FW_E 21개 셀의 V(ρ_crit) 은 52.1~73.0 km/h — **전부 80 아래.** 즉 현재 vsl_set 의 어떤 값도 FW_E 어디에서도 용량을 1 veh/h 도 못 바꾼다.

**(b) 유량 계량 창이 정확히 닫혀 있다.** 캡은 용량은 못 바꿔도 송출유량 `q = ρ·v·λ` (metanet.py:447-449) 는 줄인다 — 이게 유일한 진짜 채널이다. 그런데 h1 의 31개 제어 결정에서 **v > 80 인 결정 수**:

```
셀 5: 6/31   셀 6: 5/31   셀 7: 3/31   셀 8: 1/31   셀 9: 0/31   |   셀 10: 20/31
```

셀 8 에서 80 캡이 물릴 수 있는 결정은 **t=900 단 하나**다(v₈=112.1; t=1050 부터 65.0 이하). 그리고 컨트롤러가 seg5 에 처음 120 아래를 커밋한 시각은 **t=1800** (h1·f3 동일) — 셀 8 은 이미 16 km/h. **레버가 물릴 수 있던 창과 레버를 쓴 창이 겹치지 않는다.** 반대로 20/31 에서 캡이 물릴 수 있는 셀 10 은 구역 2 인데 31/31 전부 120 커밋이다.

**(c) 실측 정책이 안 바뀌었다.** h1 vs f3 의 FW_E VSL 은 31개 공통 결정 중 **10개에서만 다르고**, 방향이 대칭이다(h1 이 더 캡 5회 / f3 이 더 캡 5회). seg10 은 양쪽 모두 31/31 전부 120. **병목을 무장했는데 VSL 정책은 사실상 그대로다.** 이게 "여유가 생겼는가"에 대한 가장 직접적인 실측 답이다.

참고로 λ_eff 자체는 크다(occ→셀 8 최대유량 손실): occ 19(50%) −703 / 24(63%) −983 / 28(74%) −1185 / **32(84%) −1356 veh/h**. **모델에 지킬 것은 생겼는데, 지킬 도구가 없다.**

## 2. 루프는 닫혔나 — 가장 약한 화살표

```
A) 상류 VSL → q 감소            ✅ 실재 (q=ρvλ, metanet.py:447)
B) q₈ 감소 → off-ramp 도착 감소  ✅ 실재 (normal_off = ratio·q_values[i], metanet.py:552)
C) 도착 감소 → 저장고 배수       ⚠️ 모델 배수 ≤626 veh/h vs 도착 ~1000-1400 (그리고 5번 참조)
D) 배수 → λ_eff 회복 → 차로 개방  ❌ 팔로워 안에서 끊겨 있다
```

**가장 약한 화살표는 D 이고, 약한 게 아니라 설계상 절단돼 있다.** `wu_faithful_follower.py:2545/2553` 이 `occupancy=[dict(occ0) for _ in range(t_len)]` 로 지평 전체에 초기 점유를 동결하고, 주석이 명시한다 — *"occupancy(urban 소유)·유출 cap은 항상 state 동결"*. `local_freeway_plant.py:162` 의 `lanes_now = _local_lane_profile(model, occupancy, demand)` 가 바로 그 동결값을 읽는다.

**결과: 팔로워의 λ_eff 는 지평 전체·모든 후보에서 상수다.** VSL 후보가 도착을 줄여도 차로가 열리는 보상을 받을 방법이 없다. 루프는 리더/plant 롤아웃(`metanet.freeway_substep`)에서만 닫혀 있고, **레버를 실제로 고르는 주체에서는 열려 있다.**

## 3. off-ramp 배수는 무신호인가 — 아니다. 다만 모델이 믿는 신호가 아니다

`modi_eval_userfix Ver2.inpx` (sha256 085a10c7…, run_provenance 와 일치) 를 파싱해 확인:

- 커넥터 **10682** (1차로) : 본선 link 2 @2012.36 m → **link 121 @118.45 m**
- link 121 (4차로, 233.4 m) 의 유출 둘:
  - **10646** (1차로, @233.02) → link 120. **신호두 90030895, sg `9103 1` 을 단다** = 이 컨트롤러가 명령하는 **R_F_W 램프미터 RM_C10646**
  - **10773** (4차로, @233.365) → link 123. **무신호.** link 123 은 4차로 · 유출 커넥터 0개 = **sink**
- **link 68 (`SC1004_W_out`) 이 커넥터 10772(4차로)로 link 121 "안으로" 들어온다.**

**그러므로 모델의 `SC1004_p3/p4` 배수는 허구다.** config 는 OR_F_E 를 SC1004 5개 movement(p3 4개·p4 1개)로 배수한다고 선언하는데, plant 에서 SC1004 는 이 착지 링크를 **채운다.** 반면 진짜 유일한 통제 가능 배수구는 **R_F_W 램프미터**이고, 모델은 이걸 배수구로 전혀 모른다(`on_ramp_to_movement = {}`).

그리고 컨트롤러는 이 배수구를 **잠그고 있다**: `rw_meter_open_R_F_W = 0` 이 t=1500 부터, `rw_meter_green_RM_C10646` 이 t=2250/2700/4500/5400 에 3.0 s(10 중). 그 시각 10682 재차 = 28/29/21/5. 무제어에서 10682 는 전 구간 19~23 로 안정 — **기저 큐는 제어 무관하고, 제어가 깊게 만든다.**

**따라서 채널은 둘이다: 유입 제어(VSL)와 R_F_W 미터(모델이 배수구인 줄 모르는 배수 제어).**

**유입 제어의 천장 (정량):**
- 착지는 **t=600 에 이미 19/38.1 = 50%** 다 — 세 런(무제어·f3·h1) 전부 동일. 첫 제어 결정은 t=900. **컨트롤러가 첫 수를 두기 전에 절반이 차 있다.**
- 80 캡이 셀 8 에 물리는 제어 결정은 t=900 하나. 거기서 q₈ = 9.2×112.1×4 = 4125 → 80 캡시 2944, 도착(0.2×q) 825 → 589, **차이 236 veh/h × 150 s ≈ 9.8 대.**
- **천장 ≈ 10 대**, cap 38.1 대 중 이미 19 대가 차 있는 저장고에 대해. 즉 한 제어 구간 미만의 지연을 살 뿐이다.

---

# Q2 — 상류 φ 항

## 4. 이미 걸린다. 추가할 것 없다. 셀은 **7**, 그리고 그게 맞다

체인을 한 줄씩 재확인했다:

1. vendor 가 λ_eff 를 **셀 하나에만** 쓴다 — `profile[link][segment_idx] = min(profile[link][segment_idx], lambda_eff)` (metanet.py:227), `off_ramp_segment_index.OR_F_E = 8`.
2. 어댑터의 `_patched_effective_lane_profile` (adapter:8653-8674) 이 균일 base(=`freeway_lanes` 4)에서 감소분을 복원해 정적 기하에 합성하고 `_FW_SEG_CTX_STATE["profile"] = profile` 로 **게시**한다(:8673).
3. `_patched_segment_vsl` 이 그 배열을 읽어 `_dl = max(0.0, arr[i] − arr[i+1])` (adapter:8730).
4. `_patched_metanet_speed_update_kmh` 이 `_phi>0 and _dl>0` 일 때 적용(:8786-8790).

**셀 8 은 4−D₈, 셀 7 은 4 → Δλ 는 i=7 에서만 양수다. i=8 에서는 −D₈ → max(0,·) 로 0.** 오프바이원 없음, 부호 오류 없음. `_lam = arr[i]` (상류 셀 차로수) 로 표준 METANET 분모. **셀 8 자신은 용량 삭감만 받고 속도항은 안 받는다 — 이게 교과서 배치다.**

FW_W 도 같은 합성으로 셀 5(OR_D_W@6)·셀 10(OR_F_W@11)에 동적 발화점이 생긴다.

**한 가지 물리 주의:** OR_F_E 는 실제로 다이버지가 **둘**이다 — `outputs/freeway_ramp_split_v2_20260907.json`: **OR_F_E1 = 커넥터 10643, segment_index 8, 2차로** / **OR_F_E2 = 커넥터 10682, segment_index 9, 1차로**. h1 의 시드는 **10682 = 셀 9 쪽**을 잰다. 그런데 관측이 이 불일치를 오히려 정당화한다: 10643 은 전 런 0~4대 · 30~116 km/h 로 **한 번도 안 막히고**, 10682 만 22~29대 · 2.4~6.6 km/h 로 막힌다. 10682 는 본선 2012 m(= 셀 3.9?? 아니, chain 4747 m = 셀 9)에서 갈라지므로 그 출구 대기가 본선 1차로를 타고 **셀 8 로 역류한다** — 세션에서 확인한 "셀 8 에서 1차로만 죽는다"와 정확히 일치한다. **즉 차로 손실 위치(셀 8)는 맞고, 분기 유량 회계 위치가 한 셀 이르다.** 장부상 결함이지 φ 배치 결함은 아니다.

## 5. 팔로워 경로에서 **산다** — 다만 후보를 못 가른다

렌즈들이 여기서 갈렸다. 정리한다.

- `wu_distributed.py:17` 이 `from src.models.metanet import ... effective_lane_profile` 로 이름을 복사해 간다.
- `_fw_rebind` (adapter:8246-8258) 이 `sys.modules` 를 훑어 재바인딩하고, 런 메타데이터 **`fw_seg_geo_rebind = 3.0`** = metanet + distributed_coordinator + wu_distributed **정확히 셋** — 이 세 모듈이 그 이름을 가진 전부다. 게시하는 버전이 wu_distributed 에 묶여 있다.
- `mpc.relaxed_quantized_controls = bool(local_observation)` (adapter:7581) = True → `wu_faithful_follower.py:2183` 이 relaxed 분기 → **`wu_distributed.py:453` 이 모든 `_solve_freeway_agent_local` 채점 앞에서 `effective_lane_profile` 을 부른다** → 결정시점 상태로 합성 프로파일이 게시된다.

**따라서 φ 는 팔로워 채점 롤아웃 안에서 셀 7 에 실제로 걸린다.** vsl-authority 의 "발화 안 할 수도 있다(정적 기하 폴백 → Δλ=0)" 는 **기각**. phi-composition verifier 가 맞다.

**그런데 결정을 못 바꾼다.** 두 가지가 겹친다:
1. 채점 경로(`freeway_substep_local` → `_local_lane_profile`)는 **게시하지 않는다** (adapter:8687-8699 에 write 없음). 그래서 Δλ 는 그 채점 패스 내내 **모든 후보에 대해 동일한 상수**다.
2. 3번에서 본 점유 동결 때문에 후보가 λ_eff 를 움직일 방법 자체가 없다.

**남는 결과: φ 항이 VSL 에게 가짜 보상을 준다.** 항이 `speed ** 2` — **갱신 전 속도**(adapter:8789)에 비례하므로, Δλ 가 고정된 상태에서 후보가 벌점을 줄이는 유일한 길은 **v 를 낮추는 것**이다. t=1050 에 셀 7(v=106.9)에서 Δv=15.84 km/h/substep 인데, 80 으로 캡하면 (80/106.9)² = 0.56 배 → **약 7 km/h/substep 이 그냥 절약된다.** 실제 물리가 아니라 계수 하나 때문에 생긴 이득이다.

## 6. φ = 3.0 은 **틀렸다.** 관측이 맞추는 값은 **0**

**출처가 결정적이다.** `outputs/metanet_calibration_ver2n21_20260907.json` 을 직접 읽었다:

```
winner       = "cell_lit"
theta.phi    = 0.0            ← 캘리브레이션이 채택한 값
arm_results.cell_lit.phi = 0.0   (승자)
arm_results.mid2_lit.phi = 3.0   (패자)
```

그런데 라이브가 읽는 `outputs/freeway_segment_params_dlit_20260908.json` 의 `dynamics` = `{tau_sec 18, nu 30, kappa 40, delta 0.3, freeway_lane_drop_phi 3.0}` = **패배한 mid2_lit 팔의 블록 그대로**이고, `source_calibration` 이 같은 파일을 가리킨다. **살아 있는 φ 는 그 캘리브레이션이 기각한 팔의 값이다.**

**속도 결손 대조 (완주한 h1 런에서 재계산, φ=3.0, T=10/3600, L=0.513441, 셀별 ρ_crit, lane_reduction=1.0):**

| 발화점 | Δv 평균 (km/h / 10 s substep) | 최대 |
|---|---|---|
| FW_E 셀 12 (정적 4→3) | **12.06** | 24.90 |
| FW_E 셀 7 (off-ramp 신규) | **1.85** | 15.84 |

두 가지가 나온다.

**(a) 지배 발화점은 셀 7 이 아니라 셀 12 다** — 6.5배. 그리고 셀 12 는 f3 에서도 발화한다(정적 기하). 즉 landing seed 는 이미 큰 항 위에 작은 항을 하나 더 얹었을 뿐이다.

**(b) φ 항은 병목과 역상관이다.** 셀 7 의 벌점은 t=600~1200(v 106~116, **붕괴 전**)에 10~16 km/h 로 최대고, Δλ 가 최대(0.756, t=2550)일 때는 v=6.5 라 **0.64 km/h** 로 죽는다. v² 때문이다. **병목을 만들라고 넣은 항이 자유류에서만 크게 작동한다.**

**관측 정합 φ:** 두 렌즈가 독립적으로 361 샘플(셀 12, 3개 fit 런)에서 관측 결손 중앙값 −0.34 km/h 대 φ=3.0 이 부과하는 12.6 km/h → **φ_match 중앙값 ≈ +0.04, 75퍼센타일 ≈ +0.8**. 셀 7 의 f3 붕괴창(t=1170-1320)에서는 nu=30 을 켠 채로는 음수(−1.6~−5.2), nu 를 0 으로 눌러도 −0.92~+0.48. **관측이 지지하는 φ 는 0 이고 상한이 약 0.8 이다. 3.0 은 점추정의 약 75배다.** 캘리브레이션 로그 다섯 벌(v2/v3/v4/ver2n21 4arm + lit)에서 승리 팔은 다섯 번 다 `phi 0.0 → 0.0 (변화 없음)` 이었다.

**권장 브래킷: {0.0, 0.5, 3.0}.**

---

## 7. 다음 수순 (순위)

### 계측 먼저 — 코드 작업, 그러나 ~10줄 (다른 어떤 A/B 보다 먼저)

**[1] `lane_drop_fired` 와 vendor 의 `lambda_eff_*` 진단을 결정 JSON 으로 내보내라.**
`metanet.py:222-231` 이 이미 `offramp_occupancy_ratio_*` · `lambda_eff_{link}_seg{idx}` · `capacity_drop_lane_loss_*` 를 매번 만들지만, **모든 라이브 호출자가 `profile, _ = ...` 로 버린다** (`wu_distributed.py:453`, `distributed_coordinator.py:2637/3715`). `_FW_SEG_CTX_STATE["lane_drop_fired"]` (adapter:8230 초기화 / :8787 증가) 의 저장소 전체 유일한 독자는 **오프라인** `scripts/calibrate_metanet_ver2n21_20260907.py:130/152` 뿐이다. h1·f3 의 action JSON 을 전수 스캔했다 — 위 키 **0건**. **오늘 결정 한 건에서 "항이 설치됐다"와 "항이 걸렸다"를 구별할 방법이 없다.** 위 Δλ·Δv 는 전부 `landing_storage_occ_OR_F_E` + vendor 공식으로 재구성한 값이다. 셀별로 `lane_drop_fired` 를 쪼개서 내보내면 셀 7 과 셀 12 도 갈린다. **비용: 런 0회. 증명: Q2 를 코드 추적이 아니라 런 산출물로 확정.**

### config 전용 팔 (각 1런 ≈ 90분, 코드 변경 0)

**[2] `freeway.lane_drop_phi = 0.0`.** `install_freeway_lane_drop` (adapter:8614-8618) 은 오직 `tuning["freeway"]["lane_drop_phi"]` 만 읽는다(dlit 의 `dynamics` 블록은 어댑터에 소비자가 없다 — 확인함). 캘리브레이션 승자가 고른 값이고, 관측 정합값이다. **증명: φ 속도항이 순손실인지.** 주의 — φ 는 스칼라라 이 팔은 **셀 12(평균 12.06)와 셀 7(1.85)을 동시에** 끈다. 지배항을 끄는 팔이므로 사실 가장 큰 신호를 준다. **h1 은 이걸 안 시험했다.**

**[3] `freeway.vsl_zone_heads.FW_E = [0, 5, 9, 15]` (또는 `[0,4,9,15]`).** 현재 구역 1 = 셀 5–9 가 다이버지와 머지를 삼켜서, 병목으로 유입을 조이려면 병목 자신도 캡해야 한다. 머리를 9 로 옮기면 순수 상류 구역(5–8)이 생긴다. **증명: "병목에 캡이 같이 걸린다"가 손실 원인인지.** h1 미시험.

**[4] `freeway_follower.vsl_set` 에 60(및/또는 65) 추가.** 오늘 vsl_set 의 모든 값이 V(ρ_crit)=71.75 위라 **용량 권한이 정확히 0**이다. 60 → 셀 8 −2.56%(−184 veh/h), 65 → −0.86%. 두 갈래 FD 없이 VSL 에 용량 권한을 주는 **유일한 config 전용 수단**. 작지만 0 이 아니다. h1 미시험.

**[5] OR_F_E 시드 재지정.** `off_ramp_segment_index.OR_F_E` 를 8→9 로 (커넥터 10682 의 실제 위치) 하거나, `landing_storage.OR_F_E.links` 에 10643 을 추가. **증명: 한 셀 오귀속이 실제로 중요한지.** 10643 이 한 번도 안 막히므로 아마 무해하겠지만 장부는 맞춰야 한다.

### 진짜 코드 작업

**[6] 진짜 배수구를 모델에 넣어라.** OR_F_E 착지(link 121)는 plant 에서 **R_F_W 램프미터(sg 9103, 커넥터 10646)** 로 빠지고 4차로 무신호 sink(10773→123)로도 빠진다. 모델은 `SC1004_p3/p4` 로 빠진다고 믿는데, plant 의 SC1004_W_out(link 68)은 커넥터 10772 로 이 링크를 **채운다**. 지금 컨트롤러는 자기가 배수구인 줄 모르는 레버를 잠그면서(open=0 from t=1500) 병목을 스스로 깊게 만들고 있다. 매핑/토폴로지 변경.

**[7] `vsl_fd_two_branch` + `rho_crit_two_branch` 재보정.** VSL 이 v 를 자르는 대신 ρ_crit 을 움직이게 하는 교과서 기구. ρ_crit_tb ≈ 15.16 (셀 8 의 1793.7 veh/h/차로 보존)에서 80 km/h → 약 **−6%** 용량(오늘 −0.00%). **경고 둘**: (i) 재보정 없이 켜면 셀 8 용량이 118.29×25 = 2957 veh/h/차로 로 **+65% 부풀어** 프리웨이 모델 전체가 조용히 관대해진다. (ii) `rho_crit_two_branch` 는 **net 레벨 스칼라로만** 읽힌다 — 셀별 `_keys` 튜플(adapter:8638-8640)에 없고 `_patched_effective_desired_speed_kmh` (:8742-8752)가 대체하지 않으므로, 하나의 전역값이 4차로 구간과 3차로 구간을 같이 서야 한다. 제대로 하려면 셀별 배선부터.

**[8] 팔로워의 off-ramp 점유 동결 해제** (`wu_faithful_follower.py:2545/2553`). **루프를 닫는 유일한 화살표**지만, Jacobi 동결 계약(팔로워 부문제의 분리 가능성)을 깨는 변경이다. [1]·[2] 없이 손대지 마라.

**h1 이 시험한 것 / 안 한 것:** h1 은 **"λ_eff 폐쇄를 무장한다"만** 시험했고 답은 +59.6 veh·h 다. **φ 와 λ_eff 를 분리하지 못하고**([2] 필요), VSL config 변경을 하나도 시험하지 않았으며([3][4][7]), 배수구 문제를 건드리지 않았다([6]).

---

## 확립하지 못한 것 / 렌즈 불일치

- **φ 가 셀 7 에서 발화했다는 런 증거는 없다.** 코드 체인은 끝까지 확인했지만(게시→읽기→적용, 재바인딩 수 3.0/2.0/7.0 일치), 발화 자체는 관측 불가. 위 Δλ·Δv 는 전부 재구성값. → 항목 [1].
- **h1 의 +59.6 을 φ 와 λ_eff 로 못 가른다.** 같은 시드가 둘을 함께 무장한다. 그리고 +59.6 은 비짝지음 σ 50.7 의 1.2배 — 같은 시드(x15) 짝지음이라 의미는 있으나 단독으로 결정적이진 않다. VSL 정책이 거의 안 바뀌었다는 사실과 합쳐야 힘이 있다.
- **`off_ramp_split_ratio = 0.2`** (`default.yaml:163-167`)는 OR_F_E 에서 **한 번도 측정된 적이 없다** — `far_measurement` 링크 목록에 10682·10643 이 없다. 2번의 화살표 B 의 게인 전체가 이 미측정 상수에 달려 있다.
- **렌즈 불일치 판정:** ① `lane_reduction` — **1.0** (vsl-authority claim 15 기각). ② 팔로워 φ 발화 — **산다** (vsl-authority "미검증/폴백" 기각, phi-composition verifier 인용). ③ 지배 발화점 — **셀 12** (phi-composition verifier 가 맞고, phi-magnitude/whats-still-dead 의 "셀 12 는 안 막힌다"는 초반 68분에만 참). ④ VSL 권한 — vsl-authority 의 "FD 벽" 이 맞고 feedback-loop 의 6937 veh/h 는 `freeway_capacity_veh_h`(구 전역 FD 유산)를 셀 8 최대유량 7174.7 과 혼동한 것. ⑤ "배수가 무신호" — **부분적으로만 참**: 4차로 sink 경로는 무신호지만 1차로 경로는 R_F_W 미터가 물고 있고, 그건 이 컨트롤러의 레버다.
- **잠복 결함(오늘 미발동, 기록만):** adapter:8650 의 `not getattr(_mn.effective_lane_profile, "_rw_fw_seg_patch", False)` 멱등 가드는 부모에서 12219 의 landing 래퍼가 씌워진 뒤 무력화된다(래퍼에 그 속성이 없음). 부모에서 `install_freeway_segment_runtime` 이 두 번 불리면 기하가 이중 차감돼 FW_E 13-20 이 3→2 차로가 되고 셀 12 의 Δλ 가 2.0 이 된다. 오늘 안전한 이유는 12203 이 `main()` 안에 있고 워커는 `install_landing_storage_runtime` 이 `_landing_orig_effective_lane_profile` 로 되돌리기 때문뿐.

**주요 경로:** `evaluation/runs/n21x15h_h1_landing_20260909/` · `evaluation/configs/n21h_h1_20260909.json` · `evaluation/controllers/vissim_stackelberg_adapter.py` (8230/8614-8618/8653-8674/8714-8735/8763-8790/8371-8382/12201-12219) · `vendor/NumSim-mine/src/models/metanet.py` (61-96/168-232/447-470/548-570) · `vendor/NumSim-mine/src/controllers/wu_faithful_follower.py:2183,2545,2553` · `vendor/NumSim-mine/src/controllers/wu_distributed.py:17,453` · `vendor/NumSim-mine/src/controllers/local_freeway_plant.py:101-133,162` · `vendor/NumSim-mine/src/config/default.yaml:163-167,187-193` · `outputs/metanet_calibration_ver2n21_20260907.json` · `outputs/freeway_segment_params_dlit_20260908.json` · `outputs/freeway_ramp_split_v2_20260907.json` · `network/real_world_gaepo_modi/modi_eval_userfix Ver2.inpx`
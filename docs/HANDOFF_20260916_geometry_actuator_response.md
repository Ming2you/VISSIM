# 2026-09-16 기하·진출 경계·VSL/RM 응답 인계

현재 전달 기준은 [9월19일 최종 인계](HANDOFF_20260919_cost_and_ramp_response.md)다. 아래의 “commit/push 없음”은 각 실험 완료 당시의 역사적 기록이며, 이번 인계 패키지에는 해당 작업과 증거를 포함했다.

## 2026-09-19 추가: TTT 재표본화 오류와 단독 MPC 선택 수정

최신 [경계 반응·비용 집계 보고서](../diagnostics/demand_sweep/ramp_dsd_20260916_v2/merge_drain_response_20260919/REPORT.md)를 먼저 읽는다.30초 재고 표본 TTT가 작은 비용 차이의 부호를 바꾸고 단독 MPC 후보 선택에도 사용됐다. `physical_component_residence=true`로 기존 simulate/component가 본선10초·진입1초·진출 사건 적분을 사용하도록 수정했다. 비용만 바꾼12개 예측의 물리 상태가 정확히 같고,56검사·기본12개 전체 JSON·미래 관측 제거·parameter 검사 PASS다.

seed33 2400초에서10484 제한 대신10490 제한을 선택한다. 계획g8/g6/g4는 기존 VISSIM 첫450초 및 native LDP와 일치한다. 해당 구간 Ω TTT426.584028→426.421667(−0.0381%),TTD3349→3343,미확인 소실37→36이다. 장기간 성능·새 폐루프 검증은 아니며 새 런은 시작하지 않았다. 종전 동측 부분 비용의seed33 부호0/3은 내부 회계로2/3으로 정정한다. RM 실제−0.1622/모델−0.0154로 크기 오차는 여전히 크다. 과거 full GNE 정지의 원인이 모두 이 집계였다고 소급하지 않는다.

후속 단독 MPC 기준 후보는 `merge_drain_response_20260919/decisions_v1/internal_cost/`다. 진출 진입 속도·가속 옵션도 구현했지만 국소 조건부 개선과 달리450초 결합 오차는 악화되어 `entry_and_cost/`는 채택하지 않는다.10484 접속1차로는 평균 유량이 조금 늘면서 저속 재고가 줄었는데 평균q gap 상한은 반대로 낮아져, 국소 차로 상태가 다음 물리 검토 대상이다. 아래의30초 기반 수치·0/3 판단보다 이번 원래 시간 간격 집계를 우선한다. commit/push는 하지 않았다.

## 2026-09-19 추가: 본선 계수 고정·램프 보정

최신 [램프만 보정한 보고서](../diagnostics/demand_sweep/ramp_dsd_20260916_v2/ramp_response_20260919/REPORT.md)를 먼저 읽는다. **앞선 합계 해석을 정정한다.** seed33 RM에서10490의 ΔTTT는 실제+0.3625/모델+0.3011로, 이 램프의 비용은 과대평가하지 않았다. 다른 램프10484의 실제−0.2208을 모델이0으로 예측한 영향이 컸다.1초 native 기록에서도10484 정지선 이후 체류와 저속 체류가 감소한다.

본선 FD·METANET 계수를 고정하고10490/10484 수용 및 이동 시간, 도착 시간 분포를 시험했다.10484의 조건부 체류·합류 오차는 개선됐지만10490에 같은 제한을 추가하면 다른 seed에서 악화됐다. `ramp_response_20260919/arrival_v1/gap84/model/`은10484 수용 노드만 추가한 국소 후보이며,450초 예측 순비용 부호는seed23/33 모두0/3으로 여전히 실패다.150초 도착 패턴 반복도 일관된 이득이 없어 채택하지 않았다. 조건부 보정의 미래 실측 입력을 온라인 예측 정확도로 보고하지 않는다.

47검사, 현재 소스의 기존12개 예측 전체 JSON 일치,288개 요청 총량 보존,3개 미래 관측 제거,parameter 검사 PASS. 기본값은 그대로다. 새 native 런·commit·push는 없다. 다음 범위는10484의 접속 차로·platoon과 합류 대기,10483/10682의 하류 배수 반응이다. 본선 계수를 다시 전수 탐색하거나10490 대기 비용만 낮추지 않는다. 아래 기존 공간 보정 보고서의 합계 설명과10643 우선 제안보다 이번 개별 램프 분해를 우선한다.

## 2026-09-19 추가: 공간별 FD·진출 배수 시계 보정

최신 [보정 보고서](../diagnostics/demand_sweep/ramp_dsd_20260916_v2/spatial_calibration_20260919/REPORT.md)와 `spatial_calibration_20260919/QUALIFICATION.json`을 먼저 읽는다. 사용자의 보정 요청에 따라 seed13 무제어로 기하5구간의 동측 임계밀도를 보정했고, 기존 `canonical_harness.DelayedPort`에 구간 내 도착 이후의 서비스 시간만 계산하는 배수 시계 수정을 opt-in으로 연결했다. 후보는 `spatial_calibration_20260919/combined_v3/model/`이며 기본값을 바꾸지 않았다.

seed23/33의 동측 속도 RMSE는24.26→20.40 /24.80→21.50km/h,8개 진출램프450초 TTT 오차 MAE는4.18→2.87 /4.06→1.82veh·h로 개선됐다. 그러나 동측 부분 영역 RM/VSL 순비용 방향은 두 seed 모두0/3으로 실패했다. 속도 개선을 제어 이득 재현으로 해석하면 안 된다. seed33 RM 본선 ΔTTT는−0.2881(실측−0.2917)로 가까우나 램프 대기 증가와 진출 재고 감소를 잘못 예측해 순비용이 틀린다. seed23 본선 TTT 절대 오차도 약간 악화됐다.

41검사, 기본 예측12개 전체 JSON·이전8개 결과 일치, 미래 관측 제거 검사, parameter 검사 PASS. 새 VISSIM 런은 없으며 기존 검증된4조건 자료를 재사용했다. 이후 구조 보정은10643의 차로 편중·차로 이동·도시 하류 수용을 연결하는 것이 우선이다. 관련 기초 자료는 `spatial_calibration_20260919/lane_audit.json`. 이 보정본도 full GNE나 RM/VSL 성능 검증을 통과한 정본이 아니다.

## 2026-09-19 추가: 상태별 동역학 검토 및 새 seed33 검증 완료

최신 결과는 [상태·제어 응답 보고서](../diagnostics/demand_sweep/ramp_dsd_20260916_v2/state_response_20260919/REPORT.md)다. `freeway.state_response`로 방향별 relaxation τ, 밀도차별 ν, 임계밀도 전후 ν를 기존 단일 adapter에 연결했다. 기본 설정은 과거 전체 예측과 동일하며 실험 후보만 추가했다. 34개 검사,48개 설치식/진단식 비교,8개 과거 제어 예측 전체 JSON 비교 및 parameter 검사 PASS. 37개 변경 가설(반복 기준 포함40행×16예측)을 검토했지만 후보를 정본으로 채택하지 않았다.

새 seed33의 무제어/RM10490/VSL E5/동시 고정 명령4500초 런을 모두 완료했다. 2400초까지 FZP8,606,202행이 정확히 같고, LDP·초기 상태·readback·비대상 신호 검증도 통과했다. 실행 중 차량 전수 COM 조회나 추가 모델 계산 없이 기존 fast runner를 재사용했다. 현재 이 작업이 소유한 VISSIM 프로세스와 사후 분석은 모두 종료됐다.

Ω TTT는 무제어3688.809veh·h 대비 RM+8.414, VSL+45.552, 동시+40.687이다. TTD도 각각5/157/69대 감소했다. 따라서 seed23의 고정 명령 이득은 재현되지 않았다. 첫450초 동측 부분 영역에서는 작은 감소가 있었지만, VSL의 감소는 본선 이득이 아니라 주로 off-ramp 재고 감소였다. 공간별 저속 발생·전파는 보고서에 분리했다. 삭제·미삽입 문제는 여전히 남는다.

회복 τ 후보는 새 seed의 동측 ΔTTT MAE를0.1646→0.1800veh·h로 악화시켜 채택하지 않았다. `state_response_20260919/installed_v1/`의 config는 실패를 포함한 개발 후보다. 기존 `model_v5_node`도 RM/VSL 순이득 예측을 통과한 정본이 아니다. 보정의 미완료 상태를 full GNE나 새 MPC4500초 성능 검증 완료로 해석하지 않는다. seed33은 이제 본 자료이므로 후속 보정에 사용하면 다음 검증은 다른 미사용 seed가 필요하다.

다음 변경은 제어 이득을 강제하는 계수보다 병목 차로군의 재고·통과량과 램프/도시 경계 간 도착·배수 시차를 우선한다. 과거150초 시간 분포 반복도 총량을 보존하며 시험했지만 비용 방향을 해결하지 못했다. 실제 native 신호 주기가 혼재하므로 모든 경계에150초 주기를 이식하면 안 된다. 원시 FZP/LDP·오류·모델 동결 hash·실패 후보·단계별 시간은 `state_response_20260919/`에 보존했다. 이번 작업에서는 commit/push하지 않았다.

## 2026-09-19 추가: 램프 수용 모형 구현과 미통과 범위

최신 작업은 [plant 수정·검증 보고서](../diagnostics/demand_sweep/ramp_dsd_20260916_v2/plant_completion_20260919/REPORT.md) 및 같은 폴더의 `VALIDATION.json`을 따른다. 이후 사용자가 늘린 가속 거리·DSD120 망에서10681 차로별 저장/서비스와 본선 gap 수용 상한을 기존 plant에 opt-in으로 연결했다. `controller_response_4500_v1/model_v5_node`는 개발 검증 모델이며 기본값을 교체하지 않았다. 합류량 MAE는 개선됐지만 RM/VSL ΔTTT 부호 검증은 실패했다. 37검사,96개 예측,20개 기록 상태의 MPC 선택,1050초 실제 연결 검사를 마쳤고, FZP2,521,969행이 기존 무제어 앞 구간과 정확히 일치했다. 새로운 혼잡4500초 폐루프 성능이나 full GNE의 완성을 주장하지 않는다. 아래9월16일 기록의 기하/숫자는 해당 시점의 역사적 기록이다.


## 다른 컴퓨터에서 시작하기

브랜치는 `codex/control-full-review-20260909`이다. 먼저 [자료 복원 안내](../diagnostics/handoff_20260916/README.md)의 명령으로 관측자료·네트워크·실행 검증 기록을 복원한다. Git에는 소스와 보고서, 해시가 고정된 분할 자료 묶음을 함께 보관한다. 원시 FZP와 대형 native 결과 DB는 원래 컴퓨터에 남아 있으며 묶음에서 제외했다. 자료 복원과 오프라인 모델 재현에는 VISSIM이 필요 없다.

이번 인계 작업은 새 VISSIM 실험을 시작하지 않았다. 완료된 결과와 실패한 시도의 근거를 함께 보존한다. 다른 컴퓨터에서 실험을 시작하기 전에는 기존 워크스테이션의 사용 여부를 조율한다.

우선 읽을 문서:

- [전체 결과와 채택·보류 판단](../diagnostics/demand_sweep/user_native_20260914/metanet_terms_implementation_v2/REPORT_20260916.md)
- [실제 4조건 실행 결과](../diagnostics/demand_sweep/user_native_20260914/native_fixed_profile_v3/analysis/results_v1/NATIVE_FINDINGS.md)
- [램프 내부 1초 계산 변경과 검증](../diagnostics/demand_sweep/user_native_20260914/metanet_terms_implementation_v2/LOCAL_RAMP_1S_IMPLEMENTATION_20260916.md)
- [이전 controller·목적함수·8개 미터 정의](HANDOFF_20260913_controller_runtime.md)

## 고정된 네트워크와 실제 실험

기준은 사용자가 직접 수정한 망이며, 동측 본선 입력 1098만 0.8배로 줄였다. 과거의 고속도로 전체 80%·도시 50% 조건이 아니다. seed 13 기준 입력 파일은 `diagnostics/demand_sweep/user_native_20260914/east080_v1/prepared/network/baseline.inpx`이고 SHA-256은 `3795b346d7eafbbdf47ede0673050c12a4dcea2e40bca7abc085abf71381d65c`이다. seed 13·17의 9000초 결과를 보존했다.

`native_fixed_profile_v3`의 무제어·VSL·RM·동시 제어 4조건은 모두 2250초까지 완료했다. 0–1350초는 같은 native 조건, 1350–2250초는 고정된 진단 명령이다. 모든 조건의 1350초까지 FZP가 정확히 일치한다. 무제어의 2250초 전체도 기존 9000초 무제어의 앞 8,152,944개 데이터 행 및 144개 SG의 native LDP와 일치했다.

`SimPeriod=9001`을 유지하고 `SimBreak=2250`으로 끝내야 한다. 앞선 v2는 SimPeriod를 2251로 단축해 2231초부터 기준 궤적과 달라졌으며, 실패 기록을 보존했다. 새 실행에서는 이 차이를 되풀이하지 않는다.

실제 개입은 VSL 분포 59–62와 램프 connector 10490(SC 9107/SG 1)뿐이다. VSL은 1350/1500/1800/1950초에 분포 ID 100/80/100/120을 적용했다. ID는 차량 모두의 일정 속도를 뜻하지 않는다. RM은 10초 주기, 황색 없는 적색·녹색이며 1350/1500/1650/1800/1950/2100초에 녹색 8/6/4/6/8/10초를 적용했다. 진단 개입이며 full follower game의 실행 가능성을 입증한 결과가 아니다.

신호는 명령 적용 시 readback과 native LDP로 검증했다. LSA 누락 때문에 사용자가 LDP를 대체 기준으로 허용했다. 무제어의 native OFF를 임의로 GREEN 또는 검증된 g10과 동일하다고 해석하지 않는다.

| 1350–2250초 실제 결과 | 무제어 | VSL | RM | 동시 제어 |
|---|---:|---:|---:|---:|
| Ω TTT [veh·h] | 842.7581 | 843.5871 | 843.7321 | 845.7294 |
| 10490 실제 합류 [veh] | 190 | 180 | 165 | 162 |

RM이 실제 합류량과 동측 본선 체류시간을 줄인 것은 확인했다. RM의 동측 본선 TTT는 197.2308→193.3385 veh·h, 해당 램프 TTT는 2.7475→5.4264 veh·h였다. 그러나 Ω 전체 개선은 없었다. 단일 seed의 작은 비용 차이로 정책의 우열을 확정하지 않는다.

삭제·미확인 소실·미삽입이 남는다. 해당 구간 Ω 내부 미확인 소실은 조건 순서대로 72/65/62/52대, native 삭제 경고는 12/18/12/6건이며 서로 겹칠 수 있어 합산하지 않는다. 실행 종료까지 미삽입은 11151/11121/11138/11157대다. 이들을 TTD로 세지 않았다. Ω 재고 수지가 손실 없이 닫혔다고 주장하면 안 된다.

## 정본에 반영한 것과 모델의 한계

- 21개 셀의 실제 길이·차로 수를 맞췄다. 동측 4→3 차로 위치는 6841.743m로, 이전 경계보다 약 166m 하류다.
- 진출 connector의 진입·배수·이동 지연·유한 저장을 보존식으로 계산한다. 관측 재고로 예측 중간 상태를 계속 덮어쓰지 않는다.
- 기존 FD 계열을 유지하면서 seed 13으로 동역학 계수를 보정했다. seed 17은 이미 여러 번 본 개발 검증 자료이며 독립 holdout으로 부르지 않는다. 576개 방향별 450초 예측 기록에서 수치 검사는 통과했지만 E14의 저속 상태는 seed 17에서도 7/7건 놓쳤다. 속도 RMSE의 일부 감소와 달리 TTT 오차는 커졌다.
- VSL zone head가 실제 셀 10–12에만 작용하도록 맞췄다. 이전에는 셀 14까지 작용했다.
- 10490의 정지선은 272.603559m, 링크 길이는 279.032130m로 정지선 이후 공간은 약 6.43m다. 기존 10초 램프 계산의 점 대기열 문제를 진단하고, 램프 내부만 1초로 계산하도록 했다. 본선 METANET은 10초이며 계수·서비스 표는 재보정하지 않았다.
- no-option/10초 과거 결과의 수치 동일성과 새 1초 계산의 보존·저장·서비스 예산을 검증했다. 관련 24개 검사와 기본 파라미터 검사 PASS를 기록했다.

`native_response_v1`은 10초 램프 계산의 실패 근거로, `native_response_v2`는 수정 후 16개 예측 창과 동결 소스 묶음으로 보존했다. 첫 1350초 시작 450초 history 예측에서 무제어/RM 합류는 109.53/95.43대, 실제는 93/76대였다. 감소량 방향은 맞지만 절대량과 대기 비용을 정확히 설명하지 못한다. 미래 경계를 실제 값으로 준 조건부 진단에서는 92.40/83.66대이며, 이를 온라인 예측 정확도로 보고하면 안 된다.

예측 비용은 본선 셀+8개 진출 connector+10490 램프의 부분 영역이다. Ω 전체 TTT와 직접 섞지 않는다. 그 부분 영역에서도 RM 비용 차이를 모델은 거의 0으로 계산하고 후속 창에서는 부호 오류가 남는다. 10490의 본선 수용량 조회 720개가 모두 1800 veh/h이며 한 번도 결합 제약이 되지 않았다. 기하·수치 검사의 PASS는 제어 응답의 정확도 PASS가 아니다.

**새 물리 기하에서 full GNE local follower kernel의 가변 셀 길이 지원은 미완료다. 코드가 명시적으로 차단한다.** 이번 컴포넌트 실험을 통합 controller의 실행 완료로 표현하지 않는다. 기존 N_UF와 450초 합류량 허용오차를 이 단독 RM 실험의 조건으로 바꾸지 않는다.

주요 구현은 `evaluation/controllers/freeway_geometry.py`, `physical_ramp_boundary.py`, 기존 adapter/accounting/link predictor, `metanet_calibration_v1/canonical_harness.py`, `metanet_terms_implementation_v2/evaluate_native_response.py`에 있다. 실행은 기존 `fast_nc_run.ps1`·`fast_nc_runner.vbs`를 수정해 재사용했다.

## 이어서 할 일

1. 이미 완료된 4조건과 동일 초기 상태를 재사용해 목적지별 램프 도착, 정지선에서 실제 출발 가능한 차량, 본선 합류 수용량을 분리한다. 전체 램프 재고를 곧바로 서비스 가능한 큐로 취급하지 않는다.
2. 10490의 720개 수용량 계산이 모두 상한인 원인과 본선·진출 병목의 공유 여부를 검토한다. 출발지연과 차로 접근을 구분하고, 큐가 길면 서비스가 무조건 낮아진다는 보정은 넣지 않는다.
3. 실제 명령 변화에 따른 합류·재고·부분 영역 TTT 차이를 먼저 맞춘다. 추가 δ/φ 탐색이나 근거 없는 capacity drop으로 RM/VSL의 이득을 강제하지 않는다. 새 계수는 별도 상태/seed에서 검증한다.
4. 방향성과 대기 비용의 재현을 통과한 뒤 전체 follower kernel 이행과 Ω 비용 비교로 진행한다. 후보의 실행 가능성과 선택 문제는 물리 응답 검증 후 별도로 판단한다.

인계 시에는 과거 결과 JSON의 절대 경로를 바꾸지 않고, live harness가 동일한 해시의 geometry profile을 현재 저장소 안에서 찾도록 최소 경로 이식 수정을 했다. 과거 prepared receipt는 원래 실행의 출처이므로 보존한다. 다른 머신의 VISSIM 실행은 복원한 입력으로 새 출력 폴더에 다시 prepare한다.

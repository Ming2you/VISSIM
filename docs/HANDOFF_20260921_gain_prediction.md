# 2026-09-21 모델 수정 및 이득 예측 인계

Latest delivery checkpoint: `diagnostics/handoff_20260921_acceleration/README.md` supersedes the historical local-only notes below. This increment includes current-target acceleration diagnostics, rejected default-off memory/response-scale candidates, and all 60 saved local forecasts. Core/network/demand unchanged from fbdbeed; RM/VSL gain calibration remains NOT_QUALIFIED. Restore through the transport package first. Next: separate carried-velocity mixing from same-vehicle reaction error; that analysis has not been completed.


**최신 전달:** `diagnostics/handoff_20260921_transport/README.md`가 아래 인계보다 우선한다. `1fcca58` 이후 초기 차량2706쌍·공통 완료 통과900쌍·열린 말단6개 예측·국소 수송 후보를 추가 보존했다. 수송 후보는 기각하여 기본 비활성이며, 앞선 seed33 RM 순위 일치도 말단 처리 변경에 유지되지 않았다. 이득 예측은 **NOT_QUALIFIED**다. 정본 core·기준망·수요 추가 수정과 새 native 실행은 없었다. 복원·검사·다음 작업은 최신 README를 따른다.

아래는 이전 전달 시점의 누적 기록이다.

**현재 최신 재개점:** `diagnostics/handoff_20260921_matched_response/README.md`와 `K/MATCHED_SPATIAL_RESPONSE.md`가 아래 과거 인계보다 우선한다. 기존1초 적분·차로·이동거리·분기 기능을 두2550초 상태에서 비교한48개 유효 예측과 seed33 사후 본선 경계 회계를 추가했다. 이동거리·분기 후보는 두 상태의 RM 순위가 맞지만 이득 크기·구성 비용은 틀려 **NOT_QUALIFIED**다. 본체·기준망·기본 설정 추가 변경이나 새 native는 없다. 최신 복원과 후속 작업은 해당 README를 따른다.

아래는 이전 전달의 누적 기록이다.

**현재 최신 재개점:** `diagnostics/handoff_20260921_spatial/README.md`가 아래 과거 인계 문단보다 우선한다. 누적 모델 본체 수정은 `7bb685c`까지 전달했고, 이번에는 차로별 공간 수송·30초 및150초 예측·실제 미래 경계 원인 분리 진단을 추가했다. 150초 개선은 일관되지 않으며 실제 경계를 줘도2700초 이후의 국소 이득을 놓친다. 정본 추가 수정/기본 활성화/새 VISSIM 런은 없다. **순이득 보정 NOT_QUALIFIED**를 유지한다. 최신 복원·검사 명령과 다음 작업은 해당 README에 모았다.

아래는 이전 전달의 누적 기록이다.

**최신 전달:** `diagnostics/handoff_20260921_entry_speed/README.md`와 `K/RAMP_ENTRY_VELOCITY.md`가 이전 아래 기록보다 우선한다. 기존 차로군 함수에 선택적 합류 속도 입력을 추가했고47개 관련 검사 및 비활성4개 전체 예측 일치를 확인했다. 세 후보 모두 RM/VSL 순이득 예측에 실패하여 기본 채택하지 않았다. RM 관측 반복은3000초 완료, 원래9열11,790,482행 정확 일치, LDP/readback PASS다. 유효 추출 자료와 실패 기록을 모두 전달하며 판정은 NOT_QUALIFIED다. 아래의 'core 변경 없음'과 'native 없음'은 이전 커밋 단계만을 설명한다.

**이번 푸시의 최신 재개점:** `diagnostics/handoff_20260921_response/README.md`와 `K/SPEED_RESPONSE_AND_HANDOFF.md`를 먼저 읽는다. 동일 초기 차량군의 지연 경로, 실제 소비 속도식21,600회, 두 고정 하류 완화 후보 및 합류 속도·시점을 추가 확인했다. 두 후보 모두 순이득 부호를 고치지 못해 기각했다. 생산 모델은 그대로이며 보정은 NOT_QUALIFIED다. 아래 인계는 누적 구현과 이전 진단의 기록이다.

**최신 후속 인계:** `diagnostics/handoff_20260921/FOLLOWUP.md`의 복원·검사를 따른다. 누적 모델 수정은 `6d7612b`에 포함되어 있고, 이번 후속은 완료된 진단과 결과를 추가 보존한다. 동일2550초5,208대 상태의 강화 RM 추가비용은 실제+1.49861veh·h, 모델+.01349였으며 본선 효과는 반대로 예측했다. 고정 시차 반응식도 다른 개발 seed에서 실패했다. 새 native/core/기본 설정 변경은 없으며 보정 미완료다.

마지막으로 완료한 1초 본선 경계 장부는 `K/marginal_boundary_timing_v1`이다. 강화 RM의 본선 추가 체류시간은2700초−.038889,2850초+.038056,3000초+.401389veh·h로 바뀐다. 450초 말단 방출 사건의 체류시간 기여 차이+1.129167이10490 합류 사건의−.809722 등을 넘었다. 이는 보존식의 사후 회계 분해이며 독립적인 인과 효과나 새로운 예측 모델이 아니다. 초기 본선 차량676대의 추가 체류+.431389가 대부분을 차지했다. 다음에는 이 차량군의 하류16–20셀 지연 및 정상 진출 배수·도착 예측을 분리한다. 더 큰 RM 보상이나 고정 이득 계수를 넣지 않는다.

현재 판정은 **NOT_QUALIFIED: RM/VSL 순이득 보정 미완료**다. 이 커밋은 누적 모델 구현, 완료된 진단과 기각 근거를 보존한다. 미채택 후보를 성능 정본으로 승격하는 커밋이 아니다. 예전 인계의 여러 “최신” 문단보다 이 문서가 우선한다.

## 다른 컴퓨터에서 시작

- branch: `codex/control-full-review-20260909`, 이전 커밋 `342c869` 이후 증분.
- [복원 및 검사](../diagnostics/handoff_20260921/README.md)를 먼저 수행한다. 이전 09-16/09-19 증거 패키지도 필요하다.
- 아래 `H`는 `diagnostics/demand_sweep/ramp_dsd_20260916_v2`, `K`는 `H/cohort_dynamics_20260920`이다.
- 망은 사용자 가속거리 수정 + 램프 DSD120, 동측 본선 입력1098만 0.8배인 `H/source_dsd/baseline.inpx` 조건이다. 과거 전역80/도시50 조건으로 바꾸지 않는다. 개별 진단의 망 수정 후보는 별도 보존된 실험이며 기준망 변경이 아니다.
- 원본 FZP/대형 native DB/재생성 캐시는 Git에 넣지 않았다. 필요한 파일 위치·크기는 패키지 manifest의 `excluded_raw`에 있다. 저장 관측을 사용하는 오프라인 검사는 복원 가능하지만 원시 차량 재추출은 원본 파일의 별도 전송이 필요하다.

## 저장하는 모델 수정

1. `physical_lane_groups.py`: 차로군별 보존 재고·속도·수용, 목적지 재고, 실제 접속 차로와 진출 포트, 셀 내부 이동 및 분기 전후 공간 상태. 설정으로 활성화하는 진단 후보이며 기본 채택하지 않았다.
2. `physical_ramp_boundary.py`: 차로별 실제 합류 수용량, 차로 교환 시 차량·잔여 이동시간 보존, 1초 공통 시계, 1/2/5초 적분에서도 미터 주기와 위상을 보존하는 부분 주기 지원. 같은 차량을 새 서비스나 이동시간으로 이중 계산하지 않는 검사를 포함한다.
3. `desired_speed_transport.py`: DSD를 통과한 차량 집단의 희망속도 수송 후보. 차량 주행속도를 제한속도 설정값으로 취급하지 않는다.
4. 유일한 기존 adapter의 속도 문맥에 물리적으로 분리된 하위 구간 길이를 전달한다. 분기 전후 경계를 실제 차로 감소 지점으로 중복 처리하지 않는다. 기존 호출의 기본 동작은 유지한다.
5. 기존 `canonical_harness.py`, `boundary_factory.py`, 관측 추출·응답 평가 경로에 선택 설정을 연결했다. 램프/진출 대기 비용과 본선 이득을 분리한다. vendor 수정이나 adapter 복제는 없다.

각 기능의 유효 범위·기각 결과는 H의 보고서와 K의 보고서를 따른다. 단위 검사 통과는 교통 응답이나 제어 성능 보정 완료를 뜻하지 않는다. 새 기하의 full GNE 연결도 미완료다.

## 마지막으로 완료한 비교

seed23의 동일2400초 상태에서2850초까지, 동측 본선+진입4개+진출4개 connector 체류비용을 비교했다. 음수는 무제어 대비 개선이다. 도시 전체를 포함한 Ω 비용은 아니다.

| ΔTTT [veh·h] | 실제 VISSIM | 기존 진단 기준 | 하류 속도분포 결합 후보 |10483 분기 전후 분리 후보|
|---|---:|---:|---:|---:|
|RM|−0.553333|+0.004983|+0.011172|+0.010672|
|VSL|−0.981944|+0.046924|+0.051851|+0.047102|
|동시|−0.515000|+0.052045|+0.063134|+0.057966|

**두 새 후보 모두 기각했다.** RM의 본선 개선 일부는 예측하지만 램프 대기 증가를 상쇄할 정도로 예측하지 못한다. VSL의 실제 회복·방출 개선도 설명하지 못한다. 모든 제어 효과가 0인 모델은 아니지만, 순이득과 선택 순위를 신뢰할 수준은 아니다.

- 현재 native interaction target/상태를 추가한 같은 차량의 조건부1–10초 반응 검사는 오차를 조금 줄였다. 예: NC 미래검사5초 속도 RMSE12.771→12.682km/h. 매초 실제 상태를 주는 검사가450초 인과 예측의 성공을 의미하지 않는다. `K/current_interaction_response_v1`.
- 무제어 seed33의 학습을2099초에서4499초까지 넓혀 혼잡 자료를 추가했다. seed23 자체30초 예측의 일부는 나아졌지만 과도한 회복이 남았다. seed13/23/33/43은 이미 본 개발 자료이므로 독립 검증 seed가 아니다. `K/crossseed_population_v1`.
- 속도별 비음수 차량 재고를 하류15–20셀의 기존 수송에 연결해450초 네 조건까지 계산했다. 차량 보존과 가능한 분포는 유지하지만 위 순이득은 틀렸다. 비활성 대조4개 전체 JSON은 이전 결과와 정확히 같다. `K/coupled_population_work_v1`, `coupled_population_candidate_v1`.
- 기존 분기 전후 공간 분리 기능을10483 진출/10490 합류가 인접한13번 셀에도 적용했다. 원래 약493m 셀 평균 대신 현재 차량 위치에서 전후 재고·속도를 초기화했다. 무제어 복합 오차 점수는2.424895→2.392469로 소폭 개선되었으나 순이득은 실패했다. 계수 보정 없이 공간 분리만으로 해결되지 않았다. `K/merge_partition10483_v1`.

해당 결과는 [마지막 진단 요약](../diagnostics/demand_sweep/ramp_dsd_20260916_v2/cohort_dynamics_20260920/FINAL_CHECKPOINT_20260921.md)과 원본 결과 JSON에 남겼다. 앞선 실패 후보나 사용자 망 수정 대안도 삭제하지 않았다.

## 검증과 실행 상태

- 전달 전170개 단위 검사 PASS. 기존 기하에 보정된 leader_hinge/np_deadband/leader_mfd_far 경고는 남아 있으며 새 full GNE가 검증됐다고 주장하지 않는다.
- 완료 기록 재검사: 기준4개 전체 예측 JSON 일치, 후보8개 서측 기록 일치, 속도분포10,800단계 차량 보존·분포 가능성, 구성 비용 차이 재합산, 마지막 실험 중 core4파일 불변을 확인했다.
- 첫 단위 검사 호출은 Windows multiprocessing에 부적합한 stdin 방식으로 실행해 실패했다. 해당 자기 소유 세션만 종료하고 실제 파일의 main guard로 재실행하여 통과했다. 모델 실패를 숨기거나 검사를 생략한 것이 아니다.
- 마지막 오프라인 계산은 모두 종료했다. 이 푸시 작업에서는 새 VISSIM 런을 하지 않았고 다른 사용자 프로세스에 개입하지 않았다.

## 다음 작업

1. 현재 기능을 모두 켜고 성공으로 간주하지 않는다. 비교 기준은 `K/transport_step1_exchange_off_v2`와 그 config/protocol이다. 이는 진단 기준이지 채택된 최종 plant가 아니다. 최신 후보 두 개는 실패 대조군으로 유지한다.
2. 같은2400초 초기 상태에서 첫 방출·속도·재고 차이가 생기는 위치와 시간을 중심으로 최소 상태의 누락을 다시 분리한다. 조건부 속도 회귀/계수 sweep만 더 벌리지 않는다. 특히10643→126/71의 정상 배수·목적 차로 접근과10490 이후 본선 파동/회복을 구분한다. 삭제로 생긴 공간을 정상 회복으로 보정하지 않는다.
3. 작은 후보에서 RM/VSL/동시의 구성 비용·순이득·선택 순위와 무제어 보호를 함께 통과하면, 그때 미사용 seed의 짧은 실제 제어 짝 비교로 검증한다. 제어에 보상항을 붙이거나 임의 capacity drop으로 부호를 맞추지 않는다.
4. component 검증 뒤 Ω 도시 대기·TTD와8개 독립 RM·green/offset/VSL의 follower game 연결을 검증한다. Ω TTD에는 정상적으로 살아서 non-control area로 나온 사건을 포함하고 내부 이동·비정상 삭제·종료 잔여는 제외한다.

가장 큰 미해결 항목은 “코드가 제어를 적용하는가”가 아니라 **동일 초기 상태의 제어 변경이 만드는 순이득을 자체 예측하는가**다. 검토된 seed의 한 사례를 맞춘 것만으로 종료하지 않는다.

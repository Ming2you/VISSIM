# 2026-09-22 인계: 합류 상한 수정 이후 제어 이득과 선택 검증

## 재개 시 먼저 읽을 것

1. 이 문서와 `diagnostics/control_comparison_20260922/one_step_control/README.md`.
2. 최신 수치: `diagnostics/control_comparison_20260922/one_step_control/lane_capacity_v2/summary.json` 및 `capacity_response_comparison.csv`.
3. 실제4조건 결과: `diagnostics/control_comparison_20260922/README.md`.
4. 모델 연결 한계: `diagnostics/control_comparison_20260922/model_choice_probe/README.md`.

**완료 판정은 아직 NOT_QUALIFIED.** 일부 평균 속도 오차 개선, RM/VSL의 명목상ON, 아주 작은 예측 비용 절감은 실제 순이득 예측 성공과 다르다. 사용자의 핵심 요구는 “실제 제어 이득을 plant가 잡고, controller가 그 이득을 보고 선택하는가”다.

## 고정 시나리오와 완료된 작업

FW80%·도시90%, seed23,9000초,SimRes10,FZP5초. 마지막4500초 수요가9000초까지 유지된다. 원본은 `diagnostics/selected_control_demand/CURRENT_SCENARIO.json`에 SHA와 외부 경로가 있고, 이번 인계의 `diagnostics/handoff_20260922_control_choice/scenario/source/baseline.inpx`에 바이트 동일 복사본을 보존했다. Native 신호 파일은 `native_receipts/rm/network` 등에 있다. 배경 JPG는 약95MB라 제외했으며 교통 기하/수요 자체와 구분한다. 다른 컴퓨터에서 이미지 참조나 신호 파일 경로를 해결해도 교통 설정은 바꾸지 않는다.

- NC/RM/VSL/both native9000초 완료. 모든 제어 조건 LDP/readback PASS, 제어 전900초 FZP 행 완전 일치.
- Ω TTT7073.21/7012.48/7253.82/7326.03 차량·시간. RM−0.86%,VSL+2.55%,병행+3.57%. 입력1099의 미삽입이 많아 Ω 밖 대기를 무시한 전체 수요 개선이라고 말하지 않는다.
- 실제 제어는 ALINEA·규칙 VSL이며 MPC가 아니다. 세 조건162개 결정에서 MPC 선택 기록0개. 최신3플랜트 보정값은 이 규칙 정책에 사용되지 않았다.
- boundary/Hadi/Wang 재보정·held-out 예측 및 150초 재초기화 히트맵 완료. 모델이 혼잡/회복과 제어 이득을 모두 맞춘 것은 아니다. 특히 Hadi/Wang 서측 예측 악화, 회복 누락이 남아 있다.
- off-ramp 관측의 서측667개 짧은 ramp-mainline-off 통과 누락을 보존식으로 확인하고 기존 extractor를 수정했다. 현재 비교는 `offramp_dynamic_20260922/study.py::corrected_data()`가 캐시에 메모리상 적용하며 원본 관측 CSV는 보존한다.
- 단일 혼잡 상태2700.1초에서450초 예측/첫150초 명령 선택을 완료. RM한개×VSL한개 조합 최대81개/family, 전체243개 후보. 이는 작은 중앙집중 구성요소 선택기이며17도시주체 GNE나전체Ω/N_P/N_UF 검증은 아니다.

## 이번 실제 수정

1. `boundary_literature_v1/{boundary,hadi,wang}_config.json` 세 파일에 기존 `physical_ramp_capacity_vph` 설정을 명시: 2차로10482·10681 총3600대/h, 다른1차로6개1800대/h. 이전4그룹1800값이 물리8램프에 그대로 상속되던 문제를 수정했다. 실제 포화 유량을 새로 보정한 값이 아니라 명목 차로별 상한이다. 보정 coefficient는 유지했다.
2. `evaluation/controllers/physical_ramp_boundary.py`: 기존 local1초 계산이 .1초 native 시각을 거부하던 문제 수정. 소수 위상에서 녹색과 겹친 시간만큼 서비스 예산 적용. 기존 정수 시각780개 receipt JSONexact,17개 테스트PASS. 원본과 검증은one_step_control에 보존했다.
3. 실험에서는 기존 보존되는 램프 위치/이동/신호 모델 및off 저장·배수 모델을 공통 연결했다. 새로운 adapter를 만들지 않았다. 단, 기존 live MPC의 관측/비용 코드 전체를 이 새 구성에 맞춘 것은 아니다.

현재 3개 원본 config는 수정 후 값이다. 과거 pin은 `one_step_control/lane_capacity_v2/*_config.before.json`과 `capacity_change.json`을 참조해야 한다. 이전 보고서가 현재 파일 SHA와 다르다고 해서 임의로 과거 값을 덮어쓰지 않는다.

## 바로 다음 작업: 우선순위와 통과 기준

### 1. 10681의 혼잡 시 수용·합류 동역학부터 확인

2700.1초 시작 첫150초 실제 합류42대에 비해, 상한 수정 전75대/후94.24대.450초 실제152대/예측192.24대, 종료 램프 재고 실제52대/예측11.76대. 이 램프는 모델이 너무 빨리 비운다. 반면10482는450초223→291.94대(실제289), 종료재고96→27.06대(실제27)로 개선됐다. 두 램프를 다시 같은 계수로 일괄 낮추지 않는다.

먼저 저장된 포트 이벤트·cohort·본선 속도/밀도에서 신호두 이전대기, 신호두 통과, 이후 이동, 합류수용을 구분한다. 현재 receiving이 혼잡 셀의실제 차로/속도/공간을 충분히 반영하는지 추적한다. 무제어·제어의 과거 도착을 섞거나 관측 합류량을 원하는 수요로 간주하지 않는다. 실제 합류 감소와 램프 재고 증가가 함께 맞아야 한다.

허용 상한을 다시 임의로 낮춰 오차를 가리지 않는다. 필요한 경우 기존 합류·수용 계산만 수정하고 차량보존, 속도 갱신, 저장량, 자유류에서의불필요한 제한 여부까지 확인한다. 기존 연구항/추가capacity drop은 실측 근거 없이 이득을 만드는 용도로 쓰지 않는다.

### 2. RM/VSL의 비용 차이를 검증

동일 초기 상태에서 hold/RM/VSL/both를 평가한다. 선택 후보의450초본선 절감과램프·도시대기증가, 실제합류량/방출/회복, 말단재고를 함께 기록한다. 현 한계는 과거150초도착 및배수proxy, 초기 connector바깥queue 미관측, 상류도시피드백 부재다.

10482는수정후 RM계획으로합류292→210대가되지만 본선−2.489,램프+2.878,off−0.039=총+0.350차량·시간이라 선택되지 않았다. 이는레버무효와다르며, 해당비용순위가실제인지 확인할 대상이다.

현재명목선택: boundary10639RM(0.0068%),Hadi10480RM+서측입구VSL(0.0121%),Wang10480RM(0.0605%). 이 작은 차이를성공으로취급하지 않는다. VSL단독은boundary/Wang유지,Hadi의절감0.000223차량·시간뿐이다. 선택RM의첫150초누적합류도사실상동일하다. 후보전체물리효과가작은것인지,모델이예방/회복을놓치는것인지구분한다.

### 3. 기존 작은 MPC에 최신구성을 명시적으로 연결

`evaluate_response.py::mpc_choice/online_data/window`의21셀투영,정수시각전제,고정10초비용가중치와최신31셀/실제.1초/1초적분을정합한다. 현재component실험스크립트를새production adapter로복제하지말고 기존진입점을수정한다. familyconfig/계수/기하가실제로선택기에전달됐는지pin과실효값을기록한다.

먼저같은저장상태에서후보예측·비용분해·제약탈락이유·최종선택까지재현. 단순argmin결과를full GNE라고하지않는다. 그다음17도시주체,실제8램프합류기준N_UF,합의된450초±5~10대허용범위 및 N_P물리량을포함한연결을검증한다. N_P는도시경계유입+off방출−도시경계유출−on방향방출이며Ω재고나TTD로치환금지.

### 4. 짧은 native 분기 비교 후 폐루프

혼잡직전/혼잡중/회복상태에서공통이력·seed·수요·망을유지한짧은450초분기비교로예측비용차이와실제순위를확인한다. 현재네조건후기상태는이미분기했으므로동일시간자료차이를공통초기반사실비교라고하지않는다. 먼저작은중앙집중MPC에서이득과선택가능성을통과한뒤기존다중주체와비교한다.9000초fullrun은기본선행조건이아니다.

시뮬레이션은기존빠른runner,5초FZP,전환시에만COM쓰기,필수관측만유지,LDP/readback 사후검증을사용한다. 최초실제진행300초없음이면그런소유프로세스만종료한다. 별도새adapter/자동반복무한run은만들지않는다. 사용자는과한실시간모니터링보다실행후판단을선호한다.

## 계산 비용과 파일 위치

- FZP5초는기록간격,최신31셀METANET내부적분1초는다른개념이다. 최소76m셀에서5초적분이보존/밀도검사를실패해1초를유지한다. 계산비용절감명목으로검증없이5초로바꾸지않는다.
- 최신4조건5초FZP는D:/VISSIM_runs/아래이며이번push에는제외. 분석집계/히트맵/관측캐시/명령기록은포함한다.
- 기존1초FZP118개약81.57GB는E:/VISSIM_archive/20260922_fzp_1s/control-full-review/로SHA검증후이동완료. `diagnostics/fzp_archive_20260922/plan.json`으로원경로를매핑한다. C원본은삭제됐으며다른컴퓨터에서는E원본을별도복사해야한다.
- 인계`snapshot`은파일보존용이다. 스크립트내원컴퓨터절대경로와native정책/pin을다른컴퓨터에맞춰명시적으로해결하고,source hash일치와의존성존재를확인한뒤실행한다. 기존큐manifest를새컴퓨터에서그대로재시작하지않는다.
- 이인계당시관련계산은모두완료. 새native/백그라운드계산은이전달작업에서시작하지않았다. 다음작업의첫행동은위자료확인이지큐재개가아니다.
